## html的文档流是什么？举一下脱离文档流的例子，有什么目的？
HTML文档流是指元素在页面中默认的排列规则，即元素按照其在HTML结构中的顺序，从上到下、从左到右依次排列的布局方式。块级元素独占一行，行内元素在同一行内排列，当一行空间不足时自动换行。
脱离文档流的主要目的
1. 实现复杂布局 ：如层叠菜单、悬浮元素、多列布局等
2. 提高页面交互性 ：如固定导航栏、回到顶部按钮等
3. 优化视觉效果 ：如图片环绕文字、元素的动画效果等
4. 控制元素显示 ：如条件性显示/隐藏元素
通过脱离文档流，开发者可以更灵活地控制元素的位置和布局，实现更丰富的页面效果。