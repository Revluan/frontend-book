## 前端构建部署发布 (28题)

1. 什么是构建产物的增量更新？如何实现只更新变化的文件？
**增量更新**是指在应用发布新版本时，只替换（或让客户端下载）那些**内容发生变化的文件**，而不是重新部署或下载整个应用。这能大幅减少更新流量，提升发布效率和用户体验。

### 实现原理
1. **文件名哈希**  
   构建工具（如 Webpack）根据文件内容生成唯一的哈希值（如 `app.8f3d2c.js`）。内容不变，哈希不变；内容变化，哈希改变。

2. **版本清单**  
   构建时生成一个 `asset-manifest.json`（或类似文件），记录原始文件名与带哈希文件名的映射关系。服务器部署时保留历史版本文件，新版本只上传哈希变化的文件。

3. **客户端更新**  
   - 首次访问：加载 HTML，其中引用带哈希的 JS/CSS，所有文件缓存到浏览器。
   - 再次访问：HTML 可能被缓存（或协商缓存），但若新版本发布，HTML 会更新，其中引用的资源哈希发生变化，浏览器自动下载新文件，旧文件继续缓存但不被引用。

### 如何实现只更新变化的文件？
- **构建配置**：Webpack 中设置 `output.filename: '[name].[contenthash].js'`，并使用 `HtmlWebpackPlugin` 自动注入新哈希。
- **部署策略**：将带哈希的静态文件上传到 CDN，覆盖式上传（同名文件覆盖），但由于哈希变了，实际上是新文件。CDN 上会同时存在新旧文件。
- **旧文件清理**：定期清理 CDN 上不再被引用的旧文件，或使用版本化目录（如 `/v1.2.3/`）并在发布时更新软链接。

**优势**：结合 HTTP 强缓存，未变化的文件可直接从缓存加载，仅需下载变化部分，实现高效增量更新。

2. 前端如何实现蓝绿部署？切换策略如何设计？回滚方案是什么？

## 前端蓝绿部署

### 核心思路

同时维护两套完全独立的生产环境，切换时只改流量路由，旧环境保留作为回滚后备。

```
平时：  用户 → Nginx/负载均衡 → 蓝环境（当前线上 v1.1.0）
                                 绿环境（空闲）

发布：  将新版本部署到绿环境并验证
        ↓
切换：  用户 → Nginx → 绿环境（新版本 v1.2.0）
                       蓝环境（保留，随时回滚）
```

---

## 一、Nginx 切换配置

```nginx
# /etc/nginx/conf.d/frontend.conf

upstream blue {
    server blue-frontend:80;
}

upstream green {
    server green-frontend:80;
}

# 当前激活的环境（只需改这一行完成切换）
upstream active {
    server blue-frontend:80;   # ← 改为 green-frontend:80 即切换
}

server {
    listen 80;
    server_name example.com;

    location / {
        proxy_pass http://active;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # 直接访问蓝/绿环境（用于验证，不对外暴露）
    location /internal/blue/ {
        allow 10.0.0.0/8;   # 只允许内网访问
        deny all;
        proxy_pass http://blue/;
    }

    location /internal/green/ {
        allow 10.0.0.0/8;
        deny all;
        proxy_pass http://green/;
    }
}
```

切换只需两步：

```bash
# 修改 active upstream 指向，然后热重载（不中断连接）
sed -i 's/blue-frontend:80/green-frontend:80/' /etc/nginx/conf.d/frontend.conf
nginx -s reload   # 平滑重载，不会断开已有连接
```

---

## 二、Docker Compose 完整方案

```yaml
# docker-compose.yml
services:
  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/frontend.conf
    depends_on:
      - blue
      - green

  blue:
    image: my-frontend:${BLUE_VERSION:-v1.1.0}
    container_name: blue-frontend

  green:
    image: my-frontend:${GREEN_VERSION:-v1.2.0}
    container_name: green-frontend
```

```bash
# scripts/blue-green-deploy.sh

NEW_VERSION=$1
CURRENT=$(grep -o 'blue\|green' /etc/nginx/conf.d/frontend.conf | head -1)
TARGET=$([ "$CURRENT" = "blue" ] && echo "green" || echo "blue")

echo "当前环境: $CURRENT → 目标环境: $TARGET"

# 1. 更新目标环境镜像
docker compose pull ${TARGET}-frontend
${TARGET^^}_VERSION=$NEW_VERSION docker compose up -d ${TARGET}

# 2. 等待目标环境启动
sleep 5

# 3. 健康检查
HEALTH=$(curl -s -o /dev/null -w "%{http_code}" http://localhost/internal/${TARGET}/)
if [ "$HEALTH" != "200" ]; then
  echo "❌ 健康检查失败（$HEALTH），部署终止"
  exit 1
fi

# 4. 冒烟测试（可扩展）
echo "🔍 运行冒烟测试..."
npm run test:smoke -- --base-url=http://localhost/internal/${TARGET}/

# 5. 切换流量
sed -i "s/${CURRENT}-frontend/${TARGET}-frontend/" /etc/nginx/conf.d/frontend.conf
nginx -s reload

echo "✅ 切换完成！当前线上: $TARGET ($NEW_VERSION)"
echo "⚠️ 旧环境 $CURRENT 保留 30 分钟后释放"
```

---

## 三、切换策略

### 全量切换（适合大多数场景）

```
验证通过 → nginx reload → 100% 流量切换
耗时：秒级
风险：新版本有问题影响所有用户
```

### 渐进切换（高风险发布）

```nginx
# 先切 10% 流量观察
upstream active {
    server green-frontend:80 weight=1;   # 10%
    server blue-frontend:80  weight=9;   # 90%
}

# 观察 15 分钟无异常，切 50%
upstream active {
    server green-frontend:80 weight=1;
    server blue-frontend:80  weight=1;
}

# 全量切换
upstream active {
    server green-frontend:80;
}
```

### 按用户特征切换

```nginx
# 内部员工先体验新版本（通过 Header 或 Cookie 标识）
map $http_x_employee $upstream {
    "true"  green;
    default blue;
}

server {
    location / {
        proxy_pass http://$upstream;
    }
}
```

---

## 四、回滚方案

蓝绿部署的回滚极其简单，旧环境从未停止：

```bash
# scripts/rollback.sh
CURRENT=$(grep -o 'blue\|green' /etc/nginx/conf.d/frontend.conf | head -1)
FALLBACK=$([ "$CURRENT" = "blue" ] && echo "green" || echo "blue")

echo "🔄 回滚：$CURRENT → $FALLBACK"

sed -i "s/${CURRENT}-frontend/${FALLBACK}-frontend/" /etc/nginx/conf.d/frontend.conf
nginx -s reload

echo "✅ 回滚完成，耗时 < 10 秒"
```

| 回滚触发方式 | 耗时 |
|---|---|
| 手动执行脚本 | < 10 秒 |
| CI/CD 一键回滚 | < 1 分钟 |
| 监控自动触发 | < 2 分钟 |

---

## 总结

```
蓝绿部署的本质：空间换时间
  旧环境不销毁 → 回滚成本趋近于零
  切换只改路由 → 用户无感知
  新环境先验证 → 风险在切换前消除
```

蓝绿 vs 灰度的选择：流量切换要"快准狠"用蓝绿，要"渐进稳健"用灰度，两者也可以组合——先蓝绿切 10%，观察无误再全量。

3. 什么是金丝雀发布（Canary Release）？如何控制流量比例？监控指标有哪些？

## 金丝雀发布

### 什么是金丝雀发布？

名字来源于矿工用金丝雀探测矿井毒气——金丝雀对毒气敏感，先死则矿工撤退。发布同理：**让一小部分用户先"探路"，无问题再全量推送**。

```
蓝绿部署：全量切换（要么0%，要么100%）
金丝雀：  渐进切换（5% → 20% → 50% → 100%）
```

---

## 一、流量比例控制

### Nginx 权重分流

```nginx
upstream canary_pool {
    server stable-frontend:80 weight=95;  # 旧版本 95%
    server canary-frontend:80 weight=5;   # 新版本 5%
}

server {
    location / {
        proxy_pass http://canary_pool;
    }
}
```

### 基于 Cookie 的精准分流（推荐）

权重分流每次请求随机，同一用户可能切换版本。Cookie 方案保证**同一用户体验一致**：

```nginx
map $cookie_canary $upstream {
    "true"  canary-frontend:80;
    default stable-frontend:80;
}

server {
    location / {
        proxy_pass http://$upstream;

        # 对 5% 用户种下 Cookie，此后固定走金丝雀
        # 由业务服务或 lua 脚本来下发 Cookie
    }
}
```

```ts
// 前端或 BFF 控制金丝雀分配
function assignCanary(userId: string, ratio = 0.05): boolean {
  // 用 userId hash 保证同一用户结果稳定
  const hash = userId.split('').reduce((acc, c) => acc + c.charCodeAt(0), 0)
  const inCanary = (hash % 100) < (ratio * 100)

  if (inCanary) {
    document.cookie = 'canary=true; path=/; max-age=86400'
  }

  return inCanary
}
```

### 按用户特征分流

```nginx
# 内部员工 → 金丝雀（最先体验）
map $http_x_user_group $upstream {
    "internal"  canary-frontend:80;   # 内部员工
    "beta"      canary-frontend:80;   # 公测用户
    default     stable-frontend:80;   # 普通用户
}
```

---

## 二、发布节奏

```
Day 1：  内部员工（~100人）→ 观察 2 小时
Day 2：  5%  外部用户      → 观察 24 小时
Day 3：  20% 用户          → 观察 12 小时
Day 4：  50% 用户          → 观察 6 小时
Day 5：  100% 全量         → 完成
```

每个阶段未达到回滚阈值才进入下一阶段，自动化系统可替代人工判断。

---

## 三、监控指标

金丝雀发布必须对比新旧版本的关键指标，而不是只看绝对值：

```
核心指标（必须监控）：
  ├── JS 错误率         新版本 JS 报错数 / 请求数
  ├── 接口错误率        4xx / 5xx 比例
  ├── 页面加载时间      FCP、LCP、TTI
  └── 核心业务转化率    下单率、支付成功率等

对比方式：
  金丝雀用户指标  vs  稳定版用户指标
  超过阈值差异   →   自动触发回滚
```

```ts
// 前端上报时携带版本标识，监控平台按版本聚合对比
reporter.send({
  event: 'page_load',
  duration: performance.now(),
  version: __APP_VERSION__,
  isCanary: document.cookie.includes('canary=true'),
})
```

### 自动回滚阈值示例

| 指标 | 金丝雀阈值 | 触发动作 |
|---|---|---|
| JS 错误率 | 比稳定版高 > 2% | 自动回滚 |
| 接口 5xx | 比稳定版高 > 1% | 自动回滚 |
| LCP | 比稳定版慢 > 20% | 告警 + 人工确认 |
| 转化率下降 | > 5% | 自动回滚 |

---

## 总结

```
金丝雀发布 = 小流量探路 + 指标对比 + 自动回滚 + 逐步放量

关键点：
  分流要稳定（同一用户始终在同一版本）
  监控要对比（不是看绝对值，是看与稳定版的差异）
  回滚要自动（人工响应太慢，超阈值立即触发）
```

4. 前端如何实现多环境构建？dev、test、staging、prod环境如何区分配置？

## 前端多环境构建

### 核心方案：环境变量 + 模式文件

主流构建工具（Vite / Webpack / CRA）都基于 **`.env` 文件 + 构建模式** 来区分环境。

---

## 一、Vite 方案（主流）

### 1. 环境文件结构

```bash
.env                  # 所有环境共享的基础变量
.env.development      # dev 环境（vite dev 默认加载）
.env.test             # 测试环境
.env.staging          # 预发环境
.env.production       # 生产环境（vite build 默认加载）
```

### 2. 文件内容示例

```bash
# .env（公共）
VITE_APP_NAME=MyApp
VITE_APP_VERSION=1.0.0

# .env.development
VITE_API_BASE_URL=http://localhost:3000
VITE_ENABLE_MOCK=true
VITE_LOG_LEVEL=debug

# .env.test
VITE_API_BASE_URL=https://test-api.example.com
VITE_ENABLE_MOCK=false
VITE_LOG_LEVEL=info

# .env.staging
VITE_API_BASE_URL=https://staging-api.example.com
VITE_ENABLE_MOCK=false
VITE_LOG_LEVEL=warn

# .env.production
VITE_API_BASE_URL=https://api.example.com
VITE_ENABLE_MOCK=false
VITE_LOG_LEVEL=error
```

> ⚠️ Vite 中只有 `VITE_` 前缀的变量才会暴露给客户端代码，防止敏感信息泄露。

### 3. package.json 脚本配置

```json
{
  "scripts": {
    "dev":           "vite --mode development",
    "build:test":    "vite build --mode test",
    "build:staging": "vite build --mode staging",
    "build:prod":    "vite build --mode production",
    "preview":       "vite preview"
  }
}
```

### 4. 代码中使用环境变量

```ts
// 访问环境变量
const apiBase = import.meta.env.VITE_API_BASE_URL
const isDev = import.meta.env.DEV        // Vite 内置，boolean
const isProd = import.meta.env.PROD      // Vite 内置，boolean
const mode = import.meta.env.MODE        // 'development' | 'test' | 'staging' | 'production'

// 封装成配置模块 src/config/index.ts
export const config = {
  apiBase: import.meta.env.VITE_API_BASE_URL,
  enableMock: import.meta.env.VITE_ENABLE_MOCK === 'true',
  logLevel: import.meta.env.VITE_LOG_LEVEL,
}
```

---

## 二、Webpack 方案

### 配置文件拆分

```bash
config/
  webpack.common.js    # 公共配置
  webpack.dev.js       # 开发环境
  webpack.test.js      # 测试环境
  webpack.staging.js   # 预发环境
  webpack.prod.js      # 生产环境
```

```js
// webpack.prod.js
const { merge } = require('webpack-merge')
const common = require('./webpack.common.js')
const webpack = require('webpack')

module.exports = merge(common, {
  mode: 'production',
  plugins: [
    new webpack.DefinePlugin({
      'process.env.API_BASE_URL': JSON.stringify('https://api.example.com'),
      'process.env.ENABLE_MOCK': JSON.stringify('false'),
    })
  ]
})
```

```json
// package.json
{
  "scripts": {
    "dev":           "webpack serve --config config/webpack.dev.js",
    "build:test":    "webpack --config config/webpack.test.js",
    "build:staging": "webpack --config config/webpack.staging.js",
    "build:prod":    "webpack --config config/webpack.prod.js"
  }
}
```

---

## 三、各环境的典型配置差异

| 配置项 | dev | test | staging | prod |
|---|---|---|---|---|
| API 地址 | localhost | test 域名 | staging 域名 | 线上域名 |
| Mock 数据 | ✅ 开启 | ❌ 关闭 | ❌ 关闭 | ❌ 关闭 |
| SourceMap | 完整 | 完整 | 隐藏 | 关闭 |
| 代码压缩 | ❌ | ✅ | ✅ | ✅ |
| Tree Shaking | ❌ | ✅ | ✅ | ✅ |
| 调试工具 | ✅ | ✅ | ❌ | ❌ |
| 错误上报 | ❌ | ❌ | ✅ | ✅ |
| CDN 地址 | 本地 | test CDN | staging CDN | 线上 CDN |

---

## 四、工程化最佳实践

### 统一封装配置出口

```ts
// src/config/index.ts — 唯一的配置访问入口
const envConfigs = {
  development: {
    apiBase: 'http://localhost:3000',
    enableMock: true,
  },
  test: {
    apiBase: 'https://test-api.example.com',
    enableMock: false,
  },
  staging: {
    apiBase: 'https://staging-api.example.com',
    enableMock: false,
  },
  production: {
    apiBase: 'https://api.example.com',
    enableMock: false,
  },
}

const mode = import.meta.env.MODE as keyof typeof envConfigs
export const appConfig = envConfigs[mode]
```

### 敏感信息保护

```bash
# .gitignore — 本地覆盖文件不提交
.env.local
.env.*.local

# 真正的密钥存 CI/CD 环境变量，不写在 .env 文件里
```

### CI/CD 流水线对接

```yaml
# GitHub Actions 示例
jobs:
  build-staging:
    steps:
      - run: npm run build:staging
        env:
          VITE_API_BASE_URL: ${{ secrets.STAGING_API_URL }}

  build-prod:
    steps:
      - run: npm run build:prod
        env:
          VITE_API_BASE_URL: ${{ secrets.PROD_API_URL }}
```

---

### 总结

多环境构建的核心思路只有一条：**构建时注入，运行时读取**。通过 `.env` 文件 + `--mode` 参数控制变量注入，代码中通过统一的配置模块访问，敏感信息交由 CI/CD 环境变量管理，做到**代码与配置分离**，不同环境互不干扰。

5. 什么是Docker容器化部署？如何编写前端项目的Dockerfile？多阶段构建如何优化镜像大小？

## Docker 容器化部署

### 什么是容器化部署？

容器化部署是将应用程序及其**运行所需的一切**（代码、依赖、配置、运行时环境）打包成一个标准化的**镜像（Image）**，通过容器引擎运行，实现"一次构建，到处运行"。

```
传统部署：代码 + 服务器环境（手动配置，容易环境不一致）
容器化部署：代码 + 环境 = 镜像（标准化，环境完全一致）
```

**核心优势**：环境一致性、快速扩缩容、隔离性强、便于 CI/CD 集成。

---

## 一、基础 Dockerfile（不推荐用于生产）

先看一个朴素写法，理解问题所在：

```dockerfile
FROM node:18

WORKDIR /app

COPY . .

RUN npm install
RUN npm run build

EXPOSE 80

CMD ["npm", "run", "preview"]
```

**问题**：
- 镜像包含完整 Node.js + 所有 node_modules（体积可达 1GB+）
- 源码、开发依赖全部打进镜像，存在安全隐患
- 构建缓存利用率极低，每次改代码都重装依赖

---

## 二、多阶段构建（Multi-stage Build）✅ 推荐

核心思路：**构建环境和运行环境分离**，最终镜像只保留运行所需的产物。

```dockerfile
# ==================== 阶段一：依赖安装 ====================
FROM node:18-alpine AS deps

WORKDIR /app

# 单独复制 package.json，利用 Docker 层缓存
# 只要依赖没变，这一层就不会重新执行
COPY package.json package-lock.json ./

RUN npm ci --frozen-lockfile


# ==================== 阶段二：构建 ====================
FROM node:18-alpine AS builder

WORKDIR /app

# 从 deps 阶段复制已安装的依赖
COPY --from=deps /app/node_modules ./node_modules
COPY . .

# 注入构建时环境变量
ARG VITE_API_BASE_URL
ENV VITE_API_BASE_URL=$VITE_API_BASE_URL

RUN npm run build


# ==================== 阶段三：运行 ====================
# 只用轻量级 nginx 镜像，不含 Node.js
FROM nginx:1.25-alpine AS runner

# 自定义 nginx 配置（处理 SPA 路由、gzip、缓存等）
COPY nginx.conf /etc/nginx/conf.d/default.conf

# 只复制构建产物，源码和 node_modules 完全不进入最终镜像
COPY --from=builder /app/dist /usr/share/nginx/html

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
```

### nginx.conf 配置

```nginx
server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # gzip 压缩
    gzip on;
    gzip_types text/plain text/css application/javascript application/json image/svg+xml;
    gzip_min_length 1024;

    # SPA 路由支持（history 模式必须）
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 静态资源强缓存（文件名带 hash，内容不变则永久缓存）
    location ~* \.(js|css|png|jpg|svg|woff2)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # index.html 不缓存
    location = /index.html {
        add_header Cache-Control "no-cache, no-store, must-revalidate";
    }
}
```

---

## 三、多阶段构建的优化效果

```
朴素构建（单阶段）：
  node:18 基础镜像         ~950MB
  + node_modules           ~300MB
  + 源码                   ~10MB
  = 最终镜像               ~1.2GB

多阶段构建：
  nginx:1.25-alpine 基础镜像  ~40MB
  + dist 构建产物              ~5MB
  = 最终镜像                   ~45MB

体积减少约 96%
```

---

## 四、进一步优化技巧

### 1. 利用 .dockerignore 减少构建上下文

```bash
# .dockerignore
node_modules
dist
.git
.env.local
*.log
README.md
```

构建上下文越小，`docker build` 传输给 daemon 的数据就越少，构建越快。

### 2. 合理利用层缓存

Docker 每一层都会缓存，层内容未变则直接复用。关键原则是**变化频率低的层放前面**：

```dockerfile
# ✅ 正确顺序：依赖变化少，源码变化多
COPY package.json package-lock.json ./   # 层1：几乎不变
RUN npm ci                               # 层2：依赖不变时直接缓存命中
COPY . .                                 # 层3：每次改代码都变，但不影响层2
RUN npm run build                        # 层4：重新构建

# ❌ 错误顺序：每次改代码都导致 npm install 重新执行
COPY . .
RUN npm install
RUN npm run build
```

### 3. 使用 pnpm 加速依赖安装

```dockerfile
FROM node:18-alpine AS deps

RUN npm install -g pnpm

WORKDIR /app
COPY pnpm-lock.yaml package.json ./
RUN pnpm install --frozen-lockfile
```

---

## 五、常用 Docker 命令

```bash
# 构建镜像（注入环境变量）
docker build \
  --build-arg VITE_API_BASE_URL=https://api.example.com \
  -t my-frontend:1.0.0 .

# 运行容器
docker run -d -p 8080:80 --name frontend my-frontend:1.0.0

# 查看镜像大小
docker images my-frontend

# 进入容器调试
docker exec -it frontend sh
```

## 六、结合 docker-compose 多服务编排

```yaml
# docker-compose.yml
version: '3.8'

services:
  frontend:
    build:
      context: .
      args:
        VITE_API_BASE_URL: https://api.example.com
    ports:
      - "80:80"
    restart: unless-stopped
    depends_on:
      - backend

  backend:
    image: my-backend:latest
    ports:
      - "3000:3000"
```

---

### 总结

| | 单阶段 | 多阶段 |
|---|---|---|
| 镜像大小 | ~1.2GB | ~45MB |
| 安全性 | 源码暴露 | 只有产物 |
| 构建缓存 | 利用率低 | 依赖层可复用 |
| 适用场景 | 本地调试 | 生产推荐 |

多阶段构建的本质就是：**用 Node.js 构建，用 Nginx 运行**，最终镜像只交付"成品"而非"工厂"。

6. 前端如何实现静态资源的版本管理？文件指纹、版本号、时间戳的选择策略是什么？

## 前端静态资源版本管理

### 为什么需要版本管理？

浏览器和 CDN 会积极缓存静态资源，版本管理要解决的核心矛盾是：

```
缓存时间越长 → 性能越好，但更新越难感知
缓存时间越短 → 更新及时，但性能越差
```

版本管理的目标是：**让不变的资源永久缓存，让变化的资源立刻更新**。

---

## 一、三种方案对比

### 1. 时间戳（Timestamp）

在资源 URL 后拼接当前构建时间：

```html
<script src="/app.js?t=1709712000000"></script>
<link rel="stylesheet" href="/app.css?t=1709712000000">
```

```js
// webpack 简单实现
const timestamp = Date.now()
output: {
  filename: `[name].js?v=${timestamp}`
}
```

**缺点非常明显**：每次构建时间戳都变，即使文件内容完全没变，缓存也会全部失效，等于没有缓存。只适合临时调试，**不推荐生产使用**。

---

### 2. 版本号（Version）

在资源 URL 中加入语义化版本号，通常与 `package.json` 中的 version 字段联动：

```html
<script src="/v1.2.3/app.js"></script>
<!-- 或 query 形式 -->
<script src="/app.js?v=1.2.3"></script>
```

```js
// vite.config.js
import { version } from './package.json'

export default {
  base: `/${version}/`,
  build: {
    outDir: `dist/${version}`
  }
}
```

**适用场景**：整包发布、需要回滚到历史版本、Native App 内嵌 H5（版本号与 App 版本对应）。

**缺点**：粒度太粗，版本号升级会导致所有资源缓存失效，即使大部分文件没有变化。

---

### 3. 文件指纹 / contenthash（推荐）

根据文件**内容**计算 hash，内容不变则 hash 不变，内容变了 hash 才变：

```html
<!-- 构建后文件名包含 hash -->
<script src="/assets/app.8f3d2c1a.js"></script>
<link rel="stylesheet" href="/assets/app.3e7b9f2d.css">
```

```js
// vite.config.js
export default {
  build: {
    rollupOptions: {
      output: {
        // JS 入口文件
        entryFileNames: 'js/[name].[hash].js',
        // 异步分割的 chunk
        chunkFileNames: 'js/[chunk].[hash].js',
        // CSS、图片等静态资源
        assetFileNames: 'assets/[name].[hash][extname]',
      }
    }
  }
}
```

```js
// webpack.config.js
output: {
  filename: '[name].[contenthash:8].js',      // 入口
  chunkFilename: '[name].[contenthash:8].js', // 异步 chunk
}
```

> contenthash 和 chunkhash 的区别：`contenthash` 只跟单个文件内容相关，`chunkhash` 跟整个 chunk 相关。CSS 应使用 `contenthash`，避免 JS 变化带着 CSS 缓存一起失效。

---

## 二、三种方案对比总结

| | 时间戳 | 版本号 | 文件指纹 |
|---|---|---|---|
| 缓存粒度 | 全量失效 | 全量失效 | 按文件精确控制 |
| 未修改文件 | 缓存失效 ❌ | 缓存失效 ❌ | 缓存命中 ✅ |
| 回滚能力 | 无 | 支持 | 支持（hash 唯一） |
| CDN 友好 | 否 | 一般 | 最佳 |
| 实现复杂度 | 最低 | 低 | 中（构建工具支持） |
| 推荐场景 | 临时调试 | 整包/App 内嵌 | **Web 生产首选** |

---

## 三、最佳缓存策略配置

文件指纹配合 HTTP 缓存头，才能发挥最大效果：

```nginx
# nginx.conf

# index.html — 不缓存，每次都请求最新版
location = /index.html {
    add_header Cache-Control "no-cache, no-store, must-revalidate";
}

# 带 hash 的 JS/CSS — 永久缓存，内容不变则永不过期
location ~* \.(js|css)$ {
    add_header Cache-Control "max-age=31536000, immutable";
}

# 带 hash 的图片/字体
location ~* \.(png|jpg|webp|svg|woff2)$ {
    add_header Cache-Control "max-age=31536000, immutable";
}
```

**为什么 index.html 不能缓存？** 它是资源入口，引用了带 hash 的 JS/CSS 路径，必须每次获取最新版，才能让用户拿到最新的资源引用。

---

## 四、hash 粒度优化 — 避免缓存被动失效

一个常见的坑：改了业务代码，却导致第三方库的 hash 也变了。

```js
// vite.config.js — 按粒度拆分 chunk
build: {
  rollupOptions: {
    output: {
      manualChunks: {
        // 第三方库单独打包，几乎不变，可长期缓存
        vendor: ['vue', 'vue-router', 'pinia'],
        utils:  ['lodash-es', 'dayjs', 'axios'],
      }
    }
  }
}
```

```
不拆分：                        拆分后：
app.abc123.js (500KB)          vendor.111aaa.js (300KB) ← 不变，永久缓存
  ├── vue                       utils.222bbb.js  (80KB)  ← 不变，永久缓存
  ├── axios                     app.333ccc.js    (120KB) ← 只有业务代码变
  └── 业务代码                  
  
改了一行业务代码 →              改了一行业务代码 →
整个 500KB 缓存失效 ❌          只有 120KB 缓存失效 ✅
```

---

## 五、完整的版本管理方案

综合来看，生产环境推荐的完整策略是：

```
构建阶段：文件指纹（contenthash）
    ↓
缓存策略：index.html no-cache，其余 max-age=1年 + immutable
    ↓
Chunk 拆分：vendor / utils / 业务代码 分层管理
    ↓
发布流程：先上传新资源 → 再更新 index.html（灰度/CDN）
    ↓
应急手段：CDN 刷新指定路径，或 index.html 追加版本号参数
```

对于需要多版本共存的场景（如 App 内嵌 H5），可以**文件指纹 + 版本号目录结合**使用：

```
/h5/v2.3.0/assets/app.8f3d2c1a.js   ← 精确到文件级别的缓存控制
/h5/v2.3.0/assets/app.3e7b9f2d.css  ← 版本目录支持回滚
```

7. 什么是构建缓存？Webpack的cache配置如何使用？如何提升二次构建速度？

8. 前端如何实现CDN部署？CDN刷新和预热的区别是什么？
### 核心思路

前端 CDN 部署的核心是将**静态资源**（JS、CSS、图片、字体等）托管到 CDN 节点，利用就近访问加速用户请求。

### 实施步骤

**1. 资源构建 — Hash 指纹**

构建工具（Webpack/Vite）对静态资源文件名加 contenthash，实现强缓存 + 按需更新：

```js
// vite.config.js
build: {
  rollupOptions: {
    output: {
      entryFileNames: 'js/[name].[hash].js',
      chunkFileNames: 'js/[chunk].[hash].js',
      assetFileNames: 'assets/[name].[hash][extname]'
    }
  }
}
```

**2. 配置 CDN 域名**

将静态资源的 base 路径指向 CDN 域名：

```js
// vite.config.js
base: 'https://cdn.example.com/project/'
```

**3. 上传资源到 CDN**

CI/CD 流水线中将构建产物上传到 OSS/S3 等对象存储，并绑定 CDN 加速域名：

```bash
# 示例：上传到阿里云 OSS
ossutil cp -r ./dist oss://bucket/project/ --update
```

**4. HTML 单独部署**

`index.html` 不上传 CDN，而是部署到服务器并设置 `no-cache`，确保每次都能拿到最新的资源引用（带 hash 的 JS/CSS）。

**5. 设置缓存策略**

| 资源类型 | Cache-Control |
|---|---|
| index.html | `no-cache` |
| JS/CSS（有 hash） | `max-age=31536000, immutable` |
| 图片/字体（有 hash）| `max-age=31536000, immutable` |

---

## CDN 刷新 vs 预热

### CDN 刷新（Purge / 缓存清除）

**定义**：主动删除 CDN 节点上已缓存的旧资源，使节点在下次请求时回源拉取最新内容。

**使用场景**：
- 线上出现 bug，紧急发布了新版本但文件名未变
- 图片/配置文件更新后需要立刻生效
- 发布后发现用户仍访问旧资源

**流程**：
```
CDN 节点存有旧缓存
    ↓ 触发刷新
缓存标记为失效（删除）
    ↓ 用户请求到来
CDN 回源服务器拉取新资源
    ↓
缓存新资源并响应用户
```

---

### CDN 预热（Prefetch / 缓存预加载）

**定义**：在用户请求之前，提前主动将资源从源站推送并缓存到 CDN 各节点。

**使用场景**：
- 大促活动（双11、618）前，提前预热商品图片、活动页资源
- 新版本发布后，避免大量用户同时回源导致源站压力骤增
- 热门资源全球分发，降低首次访问延迟

**流程**：
```
触发预热任务
    ↓
CDN 主动回源拉取资源
    ↓
资源提前缓存到各节点
    ↓ 用户请求到来
直接命中缓存，无需回源
```

---

### 对比总结

| | CDN 刷新 | CDN 预热 |
|---|---|---|
| **方向** | 删除旧缓存 | 提前填充缓存 |
| **时机** | 资源更新后 | 资源上线前 |
| **目的** | 保证内容最新 | 保证首次命中率 |
| **对源站影响** | 刷新后首次请求会回源 | 预热时主动回源（集中一次） |
| **典型场景** | 热修复、紧急更新 | 大促活动、版本预发布 |

> 💡 **最佳实践**：新版本发布流程 = **上传新资源** → **刷新旧缓存** → **预热新资源** → **更新 HTML**，可以做到无缝切换且用户无感知。

9. 什么是Nginx的反向代理？如何配置前端项目的Nginx？gzip压缩如何开启？

## Nginx 反向代理 & 前端配置

### 什么是反向代理？

```
正向代理：客户端 → 代理 → 服务器   （代理的是客户端，如 VPN）
反向代理：客户端 → 代理 → 服务器   （代理的是服务器，如 Nginx）
```

区别在于：反向代理对客户端透明，用户不知道背后有多少台服务器，Nginx 负责转发、负载均衡、SSL 终止等。

---

### 前端项目完整 Nginx 配置

```nginx
server {
    listen 80;
    server_name example.com;
    
    # 重定向到 HTTPS
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name example.com;

    ssl_certificate     /etc/ssl/cert.pem;
    ssl_certificate_key /etc/ssl/key.pem;

    root /usr/share/nginx/html;
    index index.html;

    # ==================== gzip 压缩 ====================
    gzip on;
    gzip_min_length 1024;        # 小于 1KB 不压缩
    gzip_comp_level 6;           # 压缩级别 1-9，6 是性能与压缩率的平衡点
    gzip_vary on;                # 响应头加 Vary: Accept-Encoding
    gzip_types
        text/plain
        text/css
        application/javascript
        application/json
        image/svg+xml
        application/xml;

    # ==================== SPA 路由 ====================
    location / {
        try_files $uri $uri/ /index.html;
    }

    # ==================== 缓存策略 ====================
    # index.html 不缓存
    location = /index.html {
        add_header Cache-Control "no-cache, no-store, must-revalidate";
    }

    # 带 hash 的静态资源永久缓存
    location ~* \.(js|css|png|webp|woff2)$ {
        add_header Cache-Control "max-age=31536000, immutable";
    }

    # ==================== 反向代理 API ====================
    location /api/ {
        proxy_pass http://backend:3000/;   # 转发到后端服务
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # 超时配置
        proxy_connect_timeout 10s;
        proxy_read_timeout 30s;
    }
}
```

---

### gzip 效果

| 资源类型 | 原始大小 | gzip 后 | 压缩率 |
|---|---|---|---|
| JS | 500KB | ~150KB | 70% |
| CSS | 80KB | ~20KB | 75% |
| JSON | 100KB | ~15KB | 85% |
| 图片/二进制 | — | 几乎不变 | 不适合压缩 |

> 💡 比 gzip 更进一步：开启 **Brotli**（`brotli on`），同等质量下体积再小 15~20%，需要 Nginx 安装 `ngx_brotli` 模块。

10. 前端如何实现自动化部署？Jenkins、GitLab CI、GitHub Actions的对比是什么？

## 前端自动化部署

### 什么是自动化部署？

```
手动部署：本地构建 → 手动上传服务器 → 手动重启（慢、易出错、难回溯）
自动部署：push 代码 → CI 自动构建测试 → 自动部署上线（快、标准化、可审计）
```

---

## 一、三大平台对比

| | Jenkins | GitLab CI | GitHub Actions |
|---|---|---|---|
| **部署方式** | 自托管 | 自托管 / 云托管 | 云托管（GitHub） |
| **配置文件** | Jenkinsfile | `.gitlab-ci.yml` | `.github/workflows/*.yml` |
| **运行环境** | 自备服务器 | Runner 自备或共享 | GitHub 托管 Runner |
| **生态插件** | 1800+ 插件，最丰富 | 内置功能完善 | Marketplace 动作丰富 |
| **学习曲线** | 陡峭 | 中等 | 平缓 |
| **费用** | 服务器成本 | 免费额度 / 私有化 | 免费额度（公开库免费）|
| **适合场景** | 企业内网、复杂流水线 | GitLab 技术栈团队 | GitHub 开源 / 中小团队 |

---

## 二、GitHub Actions（最常用）

```yaml
# .github/workflows/deploy.yml
name: Build & Deploy

on:
  push:
    branches:
      - main          # 推送 main → 部署生产
      - develop       # 推送 develop → 部署测试环境
  pull_request:
    branches: [main]  # PR → 只构建不部署

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 拉取代码
        uses: actions/checkout@v4

      - name: 设置 Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: 'npm'           # 缓存 node_modules，加速后续构建

      - name: 安装依赖
        run: npm ci              # ci 比 install 更严格，锁定版本

      - name: 代码检查
        run: npm run lint

      - name: 单元测试
        run: npm run test:unit -- --run

      - name: 构建（根据分支选择环境）
        run: |
          if [ "${{ github.ref_name }}" = "main" ]; then
            npm run build:prod
          else
            npm run build:test
          fi
        env:
          VITE_API_BASE_URL: ${{ secrets.API_BASE_URL }}

      - name: 上传 CDN（只有 push 才部署，PR 不部署）
        if: github.event_name == 'push'
        run: |
          ossutil cp -r ./dist oss://my-bucket/frontend/ --update
        env:
          OSS_ACCESS_KEY_ID: ${{ secrets.OSS_KEY_ID }}
          OSS_ACCESS_KEY_SECRET: ${{ secrets.OSS_KEY_SECRET }}

      - name: 刷新 CDN 缓存
        if: github.event_name == 'push'
        run: |
          aliyun cdn RefreshObjectCaches \
            --ObjectPath "https://cdn.example.com/index.html"

      - name: 通知结果
        if: always()             # 无论成功失败都通知
        run: |
          STATUS=${{ job.status }}
          bash scripts/notify.sh $STATUS ${{ github.ref_name }} ${{ github.actor }}
```

---

## 三、GitLab CI

```yaml
# .gitlab-ci.yml
stages:
  - install
  - lint
  - test
  - build
  - deploy

variables:
  NODE_VERSION: "18"

# 缓存 node_modules，跨 job 复用
cache:
  key: ${CI_COMMIT_REF_SLUG}
  paths:
    - node_modules/

install:
  stage: install
  image: node:18-alpine
  script:
    - npm ci

lint:
  stage: lint
  script:
    - npm run lint
    - npm run tsc -- --noEmit

test:
  stage: test
  script:
    - npm run test:unit -- --run
  coverage: '/Coverage:\s(\d+\.\d+)%/'   # 提取覆盖率展示在 MR 页面

build:prod:
  stage: build
  script:
    - npm run build:prod
  artifacts:
    paths:
      - dist/          # 构建产物传递给下一个 stage
    expire_in: 1 hour
  only:
    - main

deploy:prod:
  stage: deploy
  script:
    - ossutil cp -r ./dist oss://bucket/frontend/ --update
  environment:
    name: production
    url: https://example.com
  when: manual          # 生产部署需要手动确认
  only:
    - main

deploy:test:
  stage: deploy
  script:
    - npm run build:test
    - ossutil cp -r ./dist oss://bucket/frontend-test/ --update
  environment:
    name: testing
    url: https://test.example.com
  only:
    - develop           # develop 分支自动部署测试环境
```

---

## 四、Jenkins

```groovy
// Jenkinsfile（声明式流水线）
pipeline {
    agent any

    environment {
        NODE_VERSION = '18'
        OSS_BUCKET   = 'my-bucket'
    }

    stages {
        stage('安装依赖') {
            steps {
                sh 'npm ci'
            }
        }

        stage('代码检查 & 测试') {
            parallel {
                // 并行执行，节省时间
                stage('Lint') {
                    steps { sh 'npm run lint' }
                }
                stage('单元测试') {
                    steps { sh 'npm run test:unit -- --run' }
                }
                stage('类型检查') {
                    steps { sh 'npx tsc --noEmit' }
                }
            }
        }

        stage('构建') {
            steps {
                sh "npm run build:${env.BRANCH_NAME == 'main' ? 'prod' : 'test'}"
            }
        }

        stage('部署') {
            when {
                branch 'main'   // 只有 main 分支才部署
            }
            steps {
                withCredentials([string(credentialsId: 'oss-key', variable: 'OSS_KEY')]) {
                    sh 'ossutil cp -r ./dist oss://${OSS_BUCKET}/frontend/ --update'
                }
            }
        }
    }

    post {
        success {
            sh 'bash scripts/notify.sh success ${BRANCH_NAME} ${BUILD_USER}'
        }
        failure {
            sh 'bash scripts/notify.sh failure ${BRANCH_NAME} ${BUILD_USER}'
        }
    }
}
```

---

## 五、流水线设计原则

```
快速失败：lint → test → build → deploy
          最轻的检查放最前，有问题早发现早终止

环境隔离：
  push develop → 自动部署 test 环境
  push main    → 自动部署 staging，手动确认后部署 prod

制品复用：
  构建一次 dist/ → 依次部署到 test → staging → prod
  不要每个环境重新构建（确保各环境跑的是同一份产物）

密钥管理：
  所有密钥存 CI 平台的 Secrets / 凭据管理
  绝不硬编码在配置文件里
```

---

## 总结

**怎么选？**

- 代码在 GitHub，中小团队 → **GitHub Actions**，开箱即用，生态最好
- 代码在 GitLab，或需要私有化部署 → **GitLab CI**，与仓库深度集成
- 大型企业、复杂流水线、内网隔离 → **Jenkins**，灵活性最强但维护成本高

三者流水线设计思路完全一致：**代码变更触发 → 安装依赖 → 检查测试 → 构建 → 部署 → 通知**，区别只在配置语法和运行环境。

11. 什么是构建的并行化？parallel-webpack、thread-loader如何提升构建速度？

12. 前端如何实现零停机部署？如何保证用户访问不中断？

## 前端零停机部署

### 核心思路

停机的根本原因是**新旧版本之间存在空窗期**，用户请求到达时服务不可用。零停机的关键是让新旧版本**同时存在**，流量平滑切换。

---

## 一、静态资源：天然零停机

前端使用文件指纹（contenthash）后，静态资源本身就是零停机的：

```
发布前：
  /assets/app.aaa111.js  ← 旧版本，CDN 有缓存

发布中（上传新资源）：
  /assets/app.aaa111.js  ← 旧版本，仍然存在
  /assets/app.bbb222.js  ← 新版本，已上传

更新 index.html：
  引用从 app.aaa111.js → app.bbb222.js

结果：
  正在访问的用户 → 继续用 aaa111（旧资源仍在 CDN）
  新进入的用户   → 拿到 bbb222（新资源）
```

**发布顺序至关重要**：必须先上传新资源，再更新 index.html，绝不能反过来。

```bash
# CI/CD 正确发布顺序
# 第一步：上传所有带 hash 的静态资源（JS/CSS/图片）
ossutil cp -r ./dist/assets oss://bucket/assets/ --update

# 第二步：等待 CDN 同步（可加短暂 sleep）
sleep 5

# 第三步：最后更新 index.html（此刻新版本才对用户可见）
ossutil cp ./dist/index.html oss://bucket/index.html --force

# 第四步：只刷新 index.html 的 CDN 缓存
aliyun cdn RefreshObjectCaches --ObjectPath "https://cdn.example.com/index.html"
```

---

## 二、灰度发布 / 金丝雀发布

不直接全量切换，而是先让一小部分用户访问新版本，观察无误后再逐步放量：

```
发布初期：  5% 用户 → 新版本   95% 用户 → 旧版本
观察 30min：20% 用户 → 新版本  80% 用户 → 旧版本
稳定后：    100% 用户 → 新版本
```

### Nginx 按比例分流

```nginx
upstream frontend_stable {
    server old-frontend:80;
}

upstream frontend_canary {
    server new-frontend:80;
}

# 按权重分流：5% 流量给新版本
split_clients "${remote_addr}${uri}" $variant {
    5%    canary;
    *     stable;
}

server {
    location / {
        if ($variant = "canary") {
            proxy_pass http://frontend_canary;
        }
        proxy_pass http://frontend_stable;
    }
}
```

### 基于 Cookie 的精确灰度

```nginx
# 通过 Cookie 标记灰度用户，保证同一用户体验一致
map $cookie_canary_user $upstream {
    "true"  "http://new-frontend:80";
    default "http://old-frontend:80";
}

server {
    location / {
        proxy_pass $upstream;
    }
}
```

```ts
// 前端：命中灰度的用户打上 Cookie 标记
// 后端下发灰度名单，或前端按比例随机分配
function assignCanary(ratio = 0.05): void {
  if (Math.random() < ratio) {
    document.cookie = 'canary_user=true; path=/; max-age=86400'
    location.reload()
  }
}
```

---

## 三、蓝绿部署

同时维护两套完全独立的生产环境（蓝/绿），切换时只改路由指向：

```
平时：
  用户流量 → 负载均衡 → 蓝环境（当前线上）
                        绿环境（空闲备用）

发布时：
  1. 将新版本部署到绿环境
  2. 在绿环境做完整验证
  3. 负载均衡切换：流量 → 绿环境（秒级切换）
  4. 蓝环境保留一段时间，出问题立刻切回

切换完成：
  用户流量 → 负载均衡 → 绿环境（新版本）
                        蓝环境（随时可回滚）
```

```yaml
# docker-compose 蓝绿切换示意
services:
  nginx:
    image: nginx:alpine
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf

  frontend-blue:
    image: my-frontend:v1.1.0   # 旧版本
    
  frontend-green:
    image: my-frontend:v1.2.0   # 新版本

# 切换只需修改 nginx.conf 的 proxy_pass 指向
# 然后 nginx -s reload（无停机热重载）
```

---

## 四、用户无感知的版本更新提示

即使资源零停机，已打开页面的用户仍在运行旧版本 JS，需要引导他们刷新：

```ts
// src/utils/versionChecker.ts
class VersionChecker {
  private currentVersion: string = __APP_VERSION__  // 构建时注入
  private timer: ReturnType<typeof setInterval> | null = null

  start(intervalMs = 5 * 60 * 1000) {  // 每 5 分钟检查一次
    this.timer = setInterval(() => this.check(), intervalMs)
  }

  private async check() {
    try {
      // 请求 index.html，从 etag 或自定义 header 判断版本
      const res = await fetch('/version.json?t=' + Date.now(), {
        cache: 'no-store'
      })
      const { version } = await res.json()

      if (version !== this.currentVersion) {
        this.notify()
      }
    } catch {
      // 网络异常静默处理，不影响用户
    }
  }

  private notify() {
    // 非侵入式提示，不强制刷新
    showToast({
      message: '✨ 新版本已就绪，刷新后生效',
      action: { text: '立即刷新', onClick: () => location.reload() },
      duration: 0,  // 不自动消失，等用户主动操作
    })
  }
  
  stop() {
    if (this.timer) clearInterval(this.timer)
  }
}

export const versionChecker = new VersionChecker()

// main.ts
versionChecker.start()
```

```json
// public/version.json — 每次构建时自动生成
{ "version": "1.2.0", "buildTime": "2026-02-27T10:00:00Z" }
```

---

## 五、旧资源保留策略

零停机的隐患：旧版本用户仍在使用旧 hash 的资源，如果发布时删除了旧资源会导致 404。

```bash
# 错误做法：发布时清空 CDN 目录再上传
ossutil rm -r oss://bucket/assets/ --force  # ❌ 旧用户请求 404

# 正确做法：只上传新文件，保留旧文件
ossutil cp -r ./dist/assets oss://bucket/assets/ --update  # ✅ 增量上传

# 定期清理：保留最近 N 个版本的资源
# 可用脚本对比最近3个版本的 hash 清单，删除不在任何版本中的文件
```

---

## 总结

```
零停机 = 先传资源，后更新 HTML（文件指纹保障）
       + 灰度/蓝绿 平滑切换流量
       + 版本检测 引导存量用户刷新
       + 旧资源保留 防止 404
```

| 方案 | 适用场景 | 切换速度 | 回滚速度 |
|---|---|---|---|
| 文件指纹 + 顺序发布 | 所有前端项目，必做基础 | 秒级 | 秒级 |
| 灰度发布 | 高风险功能，大流量产品 | 渐进（分钟~小时） | 分钟级 |
| 蓝绿部署 | 对可用性要求极高的系统 | 秒级切换 | 秒级 |
| 版本更新提示 | 存量用户体验保障 | 用户主动触发 | — |

13. 什么是构建产物的分析？webpack-bundle-analyzer如何使用？如何识别打包问题？

## 构建产物分析

### 为什么需要分析？

构建后不知道包里装了什么，遇到"包太大"只能瞎猜。Bundle 分析让你**看清每个依赖占多少体积**，精准优化。

---

## 一、webpack-bundle-analyzer 使用

```bash
npm install -D webpack-bundle-analyzer
```

```js
// webpack.config.js
const { BundleAnalyzerPlugin } = require('webpack-bundle-analyzer')

module.exports = {
  plugins: [
    new BundleAnalyzerPlugin({
      analyzerMode: 'static',      // 生成 HTML 文件，不自动开启服务
      reportFilename: 'report.html',
      openAnalyzer: false,
      generateStatsFile: true,     // 同时生成 stats.json（可二次分析）
    })
  ]
}
```

```json
// package.json — 按需触发，不影响日常构建
{
  "scripts": {
    "analyze": "cross-env ANALYZE=true npm run build"
  }
}
```

```js
// 也可以用环境变量控制是否启用
plugins: [
  process.env.ANALYZE && new BundleAnalyzerPlugin()
].filter(Boolean)
```

**Vite 对应工具：**

```bash
npm install -D rollup-plugin-visualizer
```

```ts
// vite.config.ts
import { visualizer } from 'rollup-plugin-visualizer'

export default {
  plugins: [
    visualizer({
      open: true,          // 构建后自动打开
      gzipSize: true,      // 显示 gzip 后体积
      brotliSize: true,
      filename: 'stats.html',
    })
  ]
}
```

---

## 二、如何读懂分析报告

```
报告是一个矩形树图：
  ├── 面积越大 = 体积越大
  ├── 颜色区分不同 chunk
  └── 可点击下钻查看内部构成

三种体积指标：
  Stat size    原始体积
  Parsed size  webpack 处理后体积
  Gzip size    实际传输体积（最重要）
```

---

## 三、常见打包问题识别

### 问题一：第三方库体积过大

```
现象：vendor chunk 占 80% 以上
常见元凶：moment.js（含所有语言包）、lodash（全量引入）

# 分析报告中看到 moment/locale 目录巨大
解决：
  moment → dayjs（体积小 97%）
  lodash → lodash-es + tree shaking
  import { debounce } from 'lodash'      # ❌ 引入整个 lodash
  import debounce from 'lodash/debounce' # ✅ 按需引入
```

### 问题二：重复依赖

```
现象：同一个库出现多次（不同版本）
      react 16.x 和 react 17.x 同时存在

解决：
// webpack.config.js — 强制统一版本
resolve: {
  alias: {
    'lodash': path.resolve(__dirname, 'node_modules/lodash')
  }
}
```

### 问题三：没有 Tree Shaking

```
现象：引入一个工具函数，整个库都打进去了

原因：库使用 CommonJS 格式（require），无法 tree shaking
     只有 ESM 格式（import/export）才支持

解决：优先选择提供 ESM 版本的库
  antd → 配置 babel-plugin-import 按需加载
  echarts → import { LineChart } from 'echarts/charts' 按需引入
```

### 问题四：chunk 拆分不合理

```
现象：
  app.js 5MB（所有代码打在一起）
  或者 chunk 数量过多（每个组件一个文件，HTTP 请求爆炸）

解决：合理配置 splitChunks
```

```js
// webpack splitChunks 配置
optimization: {
  splitChunks: {
    chunks: 'all',
    cacheGroups: {
      // 稳定的大型框架单独打包，长期缓存
      vendor: {
        name: 'vendor',
        test: /[\\/]node_modules[\\/](react|vue|antd)[\\/]/,
        priority: 20,
      },
      // 其余第三方库合并一个包
      commons: {
        name: 'commons',
        test: /[\\/]node_modules[\\/]/,
        priority: 10,
        minChunks: 2,    // 至少被引用 2 次才提取
      }
    }
  }
}
```

### 问题五：图片/字体未优化

```
现象：assets chunk 里有大量 base64 图片（内联进了 JS）

原因：url-loader limit 设置太高，大图也被 base64 内联

解决：
// 只内联 < 4KB 的小图，大图走独立文件
{
  test: /\.(png|jpg|gif)$/,
  type: 'asset',
  parser: {
    dataUrlCondition: { maxSize: 4 * 1024 }  // 4KB
  }
}
```

---

## 四、快速定位流程

```
打开分析报告
    │
    ├── 找最大的色块 → 是第三方库？→ 能否替换更小的？能否按需加载？
    │
    ├── 同一库出现多次 → 重复依赖 → resolve.alias 强制统一
    │
    ├── 业务代码 chunk 过大 → 路由懒加载 / 动态 import 拆分
    │
    └── gzip 后仍然 > 200KB → 考虑 CDN 外链（不打进 bundle）
```

```js
// 路由懒加载 — 最直接的拆包手段
const routes = [
  {
    path: '/dashboard',
    component: () => import('./pages/Dashboard.vue')  // 访问时才加载
  }
]
```

---

## 总结

| 问题 | 识别方式 | 解决方案 |
|---|---|---|
| 库体积过大 | 色块面积占比高 | 替换轻量库 / 按需引入 |
| 重复依赖 | 同库出现多次 | resolve.alias 统一 |
| Tree Shaking 失效 | 工具函数带入整库 | 改用 ESM 格式库 |
| chunk 不合理 | 单文件过大或过碎 | splitChunks 分层配置 |
| 图片内联过多 | assets 在 JS 里 | 调小 dataUrlCondition |

14. 前端如何实现版本回滚？回滚策略如何设计？数据兼容性如何处理？

## 前端版本回滚

### 为什么前端回滚比后端复杂？

后端回滚只需切换服务版本，前端还要面对：

```
浏览器缓存    → 用户可能还在跑旧版/新版代码
localStorage  → 新版本写入的数据结构，旧版本读不懂
Cookie/Token  → 新旧版本的认证格式可能不同
CDN 缓存      → 各节点缓存状态不一致
```

---

## 一、静态资源回滚

### 多版本并存是回滚的基础

```
CDN 存储结构：
  /frontend/
    ├── v1.1.0/
    │     ├── index.html
    │     └── assets/app.aaa111.js
    ├── v1.2.0/
    │     ├── index.html
    │     └── assets/app.bbb222.js
    └── current → 指向当前版本（软链接/配置）
```

回滚本质上只是**把 current 指向改回旧版本**，旧资源从未删除，所以可以秒级生效。

### 回滚脚本

```bash
#!/bin/bash
# scripts/rollback.sh

TARGET_VERSION=$1
BUCKET="oss://prod-bucket/frontend"
CDN_DOMAIN="https://cdn.example.com"

if [ -z "$TARGET_VERSION" ]; then
  echo "❌ 请指定回滚版本，例如：./rollback.sh v1.1.0"
  exit 1
fi

# 检查目标版本是否存在
VERSION_EXISTS=$(ossutil ls $BUCKET/$TARGET_VERSION/ 2>&1)
if [[ $VERSION_EXISTS == *"No"* ]]; then
  echo "❌ 版本 $TARGET_VERSION 不存在"
  exit 1
fi

echo "🔄 开始回滚到 $TARGET_VERSION..."

# 将目标版本的 index.html 覆盖到根路径
ossutil cp \
  $BUCKET/$TARGET_VERSION/index.html \
  $BUCKET/index.html \
  --force

# 只刷新 index.html 的 CDN 缓存（静态资源有 hash，无需刷新）
aliyun cdn RefreshObjectCaches \
  --ObjectPath "$CDN_DOMAIN/index.html" \
  --ObjectType File

# 记录回滚日志
echo "$(date): rollback to $TARGET_VERSION by $USER" >> ./deploy.log

echo "✅ 回滚完成！当前版本：$TARGET_VERSION"
echo "📊 请监控错误率是否下降..."
```

### CI/CD 集成一键回滚

```yaml
# .github/workflows/rollback.yml
name: Rollback

on:
  workflow_dispatch:
    inputs:
      target_version:
        description: '回滚目标版本（如 v1.1.0）'
        required: true
      reason:
        description: '回滚原因'
        required: true

jobs:
  rollback:
    runs-on: ubuntu-latest
    environment: production  # 需要审批才能执行

    steps:
      - uses: actions/checkout@v3

      - name: 执行回滚
        run: bash scripts/rollback.sh ${{ inputs.target_version }}
        env:
          OSS_ACCESS_KEY: ${{ secrets.OSS_ACCESS_KEY }}

      - name: 通知团队
        run: |
          curl -X POST ${{ secrets.DINGTALK_WEBHOOK }} \
            -d '{
              "text": {
                "content": "⚠️ 生产回滚：${{ inputs.target_version }}\n原因：${{ inputs.reason }}\n操作人：${{ github.actor }}"
              }
            }'
```

---

## 二、回滚策略设计

### 版本保留策略

```
不能无限保留历史版本（存储成本），但保留太少又无法回滚

推荐策略：
  ├── 最近 5 个版本  → 完整保留（随时可回滚）
  ├── 更早的版本     → 只保留 index.html（资源可重新构建）
  └── 90天以前       → 完全清理
```

```bash
# scripts/cleanup-old-versions.sh
# 保留最近 N 个版本，清理更早的

KEEP_VERSIONS=5
BUCKET="oss://prod-bucket/frontend"

# 获取所有版本，按时间排序
ALL_VERSIONS=$(ossutil ls $BUCKET/ --limited-num 100 | grep "v[0-9]" | sort -r)

# 跳过前 N 个，删除其余
echo "$ALL_VERSIONS" | tail -n +$((KEEP_VERSIONS + 1)) | while read version; do
  echo "🗑️ 清理旧版本：$version"
  ossutil rm -r "$version" --force
done
```

### 自动回滚触发条件

```ts
// 监控系统：错误率超阈值自动触发回滚
// monitor/autoRollback.ts

const ROLLBACK_RULES = {
  errorRate: {
    threshold: 0.05,      // 错误率 > 5% 触发
    windowMs: 5 * 60000,  // 5 分钟窗口
  },
  p99Latency: {
    threshold: 3000,      // P99 响应时间 > 3s 触发
    windowMs: 3 * 60000,
  },
  jsErrorCount: {
    threshold: 100,       // 5 分钟内 JS 报错 > 100 次触发
    windowMs: 5 * 60000,
  }
}

async function checkAndRollback(currentVersion: string, prevVersion: string) {
  const metrics = await fetchMetrics(ROLLBACK_RULES.errorRate.windowMs)

  const shouldRollback =
    metrics.errorRate > ROLLBACK_RULES.errorRate.threshold ||
    metrics.p99 > ROLLBACK_RULES.p99Latency.threshold ||
    metrics.jsErrors > ROLLBACK_RULES.jsErrorCount.threshold

  if (shouldRollback) {
    console.error('🚨 指标异常，触发自动回滚', metrics)

    // 调用回滚 API
    await triggerRollback(prevVersion)

    // 告警通知
    await notify(`自动回滚至 ${prevVersion}，原因：错误率 ${metrics.errorRate}`)
  }
}
```

---

## 三、数据兼容性处理（最复杂的部分）

回滚最大的风险不在代码，而在**数据结构变了之后代码回滚，旧代码读不懂新数据**。

### localStorage 数据迁移

```ts
// src/storage/migrations.ts

type Migration = {
  version: number
  up: (data: any) => any    // 升级
  down: (data: any) => any  // 降级（支持回滚）
}

const migrations: Migration[] = [
  {
    version: 2,
    // v1 → v2：userInfo 从扁平结构改为嵌套结构
    up: (data) => ({
      ...data,
      userInfo: {
        basic: { name: data.userName, email: data.userEmail },
        settings: { theme: data.theme ?? 'light' }
      },
      // 清除旧字段
      userName: undefined,
      userEmail: undefined,
      theme: undefined,
    }),
    // v2 → v1：展开嵌套结构（支持回滚）
    down: (data) => ({
      ...data,
      userName: data.userInfo?.basic?.name,
      userEmail: data.userInfo?.basic?.email,
      theme: data.userInfo?.settings?.theme ?? 'light',
      userInfo: undefined,
    }),
  },
  {
    version: 3,
    // v2 → v3：购物车数据结构变更
    up: (data) => ({
      ...data,
      cart: (data.cart ?? []).map((item: any) => ({
        ...item,
        addedAt: item.addedAt ?? Date.now(),  // 新增字段给默认值
      }))
    }),
    down: (data) => ({
      ...data,
      cart: (data.cart ?? []).map(({ addedAt, ...rest }: any) => rest)
    }),
  }
]

class StorageMigrationManager {
  private STORAGE_KEY = 'app_data'
  private VERSION_KEY = 'app_data_version'
  private CURRENT_VERSION = 3  // 当前版本

  // 应用启动时执行迁移
  migrate() {
    const storedVersion = Number(localStorage.getItem(this.VERSION_KEY) ?? 1)
    const data = JSON.parse(localStorage.getItem(this.STORAGE_KEY) ?? '{}')

    if (storedVersion === this.CURRENT_VERSION) return

    let migratedData = data

    if (storedVersion < this.CURRENT_VERSION) {
      // 升级：依次执行 up
      const pendingMigrations = migrations.filter(
        m => m.version > storedVersion && m.version <= this.CURRENT_VERSION
      )
      migratedData = pendingMigrations.reduce((d, m) => m.up(d), data)

    } else {
      // 降级（回滚场景）：依次逆序执行 down
      const pendingMigrations = migrations
        .filter(m => m.version > this.CURRENT_VERSION && m.version <= storedVersion)
        .reverse()
      migratedData = pendingMigrations.reduce((d, m) => m.down(d), data)
    }

    localStorage.setItem(this.STORAGE_KEY, JSON.stringify(migratedData))
    localStorage.setItem(this.VERSION_KEY, String(this.CURRENT_VERSION))
  }
}

// main.ts — 应用启动时第一步执行
new StorageMigrationManager().migrate()
```

### API 接口向后兼容

回滚后旧版前端可能调用新版 API，接口设计需要保持兼容：

```ts
// ✅ 正确：新增字段给默认值，不删除旧字段
// API v1 返回：{ name: 'Alice', age: 18 }
// API v2 返回：{ name: 'Alice', age: 18, avatar: '' }  ← 只新增，不删除

// ❌ 错误：直接修改字段结构
// API v2 返回：{ username: 'Alice', age: 18 }  ← 字段改名，旧版前端崩溃

// 接口版本化：实在无法兼容时，开新版本接口
// /api/v1/user  ← 旧版前端继续用
// /api/v2/user  ← 新版前端用，旧接口不下线直到确认无流量
```

### Cookie / Token 格式兼容

```ts
// src/auth/tokenAdapter.ts
// 新版 Token 格式变更时，兼容旧格式解析

interface TokenV1 { uid: string; exp: number }
interface TokenV2 { userId: string; expiredAt: number; roles: string[] }

function parseToken(token: string): TokenV2 {
  const payload = JSON.parse(atob(token.split('.')[1]))

  // 判断是 v1 还是 v2 格式，统一转换为 v2
  if (payload.uid !== undefined) {
    // 旧格式，适配为新格式
    return {
      userId: payload.uid,
      expiredAt: payload.exp,
      roles: [],  // 旧版无此字段，给默认值
    }
  }

  return payload as TokenV2
}
```

---

## 四、回滚演练

回滚机制要定期演练，否则真正需要时才发现流程有问题：

```
每月一次回滚演练：
  1. 在 staging 环境执行回滚脚本
  2. 验证旧版本功能正常
  3. 验证数据迁移降级正确执行
  4. 记录回滚耗时（目标 < 5 分钟）
  5. 复盘并更新回滚手册
```

---

## 总结

```
回滚能力 = 多版本并存（文件指纹 + 目录结构）
         + 一键回滚脚本（CDN 切换 index.html）
         + 自动触发（监控指标阈值）
         + 数据降级迁移（localStorage migration）
         + API 向后兼容（不删旧字段，版本化接口）
```

| 回滚层面 | 方案 | 目标耗时 |
|---|---|---|
| 静态资源 | 覆盖 index.html + 刷新 CDN | < 1 分钟 |
| 应用服务 | 蓝绿切换 / 容器回滚 | < 2 分钟 |
| 本地存储 | Migration down 自动执行 | 毫秒级 |
| 接口兼容 | 向后兼容设计 + 接口版本化 | 无需操作 |
| 全链路 | 自动化脚本 + CI/CD 审批 | < 5 分钟 |

15. 什么是构建的持久化缓存？Webpack 5的filesystem cache如何配置？

16. 前端如何实现按需构建？只构建变化的页面或模块？

17. 什么是静态资源的跨域配置？CORS头在Nginx中如何设置？

## 静态资源跨域配置

### 什么是跨域？

浏览器的**同源策略**限制：协议、域名、端口三者任一不同，就是跨域，请求会被浏览器拦截。

```
https://app.example.com  →  https://api.example.com   ❌ 域名不同
https://example.com      →  http://example.com         ❌ 协议不同
https://example.com:80   →  https://example.com:443    ❌ 端口不同
https://example.com      →  https://example.com        ✅ 同源
```

> 注意：跨域拦截发生在**浏览器端**，请求实际上已经到达服务器，只是响应被浏览器拦截了。服务器通过响应头告诉浏览器"我允许你访问"。

---

### Nginx CORS 配置

```nginx
server {
    listen 443 ssl http2;
    server_name api.example.com;

    # ==================== CORS 配置 ====================

    # 处理预检请求（OPTIONS）
    # 浏览器在发送非简单请求前，先发 OPTIONS 探路
    location / {
        # 允许的来源（生产环境不要用 * ，明确指定域名）
        add_header Access-Control-Allow-Origin  "https://app.example.com" always;

        # 允许携带 Cookie / Authorization（使用此项时 Allow-Origin 不能为 *）
        add_header Access-Control-Allow-Credentials "true" always;

        # 允许的请求方法
        add_header Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS" always;

        # 允许的请求头
        add_header Access-Control-Allow-Headers "Authorization, Content-Type, X-Request-ID" always;

        # 预检结果缓存时间（秒），缓存期内不再发 OPTIONS
        add_header Access-Control-Max-Age "86400" always;

        # 允许前端读取的响应头（默认只能读到基础头）
        add_header Access-Control-Expose-Headers "X-Total-Count, X-Request-ID" always;

        # 拦截 OPTIONS 预检请求，直接返回 204，不走业务逻辑
        if ($request_method = OPTIONS) {
            return 204;
        }

        proxy_pass http://backend:3000;
    }
}
```

---

### 多域名动态 Allow-Origin

`Access-Control-Allow-Origin` 只能设置一个值，多域名需要动态匹配：

```nginx
# 维护允许的域名白名单
map $http_origin $cors_origin {
    default                          "";
    "https://app.example.com"        $http_origin;
    "https://admin.example.com"      $http_origin;
    "https://m.example.com"          $http_origin;
    # 本地开发环境
    "http://localhost:3000"          $http_origin;
    "http://localhost:5173"          $http_origin;
}

server {
    location /api/ {
        add_header Access-Control-Allow-Origin  $cors_origin always;
        add_header Access-Control-Allow-Credentials "true" always;

        if ($request_method = OPTIONS) {
            add_header Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS";
            add_header Access-Control-Allow-Headers "Authorization, Content-Type";
            add_header Access-Control-Max-Age "86400";
            return 204;
        }

        proxy_pass http://backend:3000/;
    }
}
```

---

### 静态资源跨域（字体/图片）

字体文件（woff2）跨域是最常见的静态资源跨域问题：

```nginx
server {
    server_name cdn.example.com;
    root /usr/share/nginx/html;

    # 字体文件跨域（被其他域名的页面引用字体时必须配置）
    location ~* \.(woff|woff2|ttf|eot|otf)$ {
        add_header Access-Control-Allow-Origin "*";  # 字体资源一般可以用 *
        add_header Cache-Control "max-age=31536000, immutable";
    }

    # 图片/媒体资源（如需 JS 通过 fetch 访问）
    location ~* \.(png|jpg|webp|svg|mp4)$ {
        add_header Access-Control-Allow-Origin "*";
        add_header Cache-Control "max-age=31536000, immutable";
    }
}
```

---

### 前端开发环境代理（绕过跨域）

开发阶段不改 Nginx，直接用构建工具代理：

```ts
// vite.config.ts
export default {
  server: {
    proxy: {
      '/api': {
        target: 'https://api.example.com',
        changeOrigin: true,       // 修改请求头中的 Origin
        rewrite: path => path.replace(/^\/api/, ''),
      }
    }
  }
}
```

请求 `/api/user` → 代理到 `https://api.example.com/user`，浏览器看到的始终是本地地址，不触发跨域。

---

### 简单请求 vs 预检请求

```
简单请求（直接发送，无 OPTIONS）：
  方法：GET / POST / HEAD
  请求头：只含 Content-Type: text/plain | form-data | urlencoded

预检请求（先发 OPTIONS）：
  方法：PUT / DELETE / PATCH
  请求头：含 Authorization / Content-Type: application/json / 自定义头
  → 浏览器先问服务器"我能不能发？"，服务器回答后再发真实请求
```

---

### 常见错误排查

| 错误信息 | 原因 | 解决 |
|---|---|---|
| `No 'Access-Control-Allow-Origin'` | 服务器没有返回 CORS 头 | 检查 Nginx 配置是否生效 |
| `Allow-Origin` 不能为 `*` 同时带 `Credentials` | 规范限制 | 明确指定域名，不用 `*` |
| 预检请求 404 | OPTIONS 没有对应路由 | 添加 OPTIONS 处理，返回 204 |
| 响应头读不到 | 头没有在 `Expose-Headers` 声明 | 补充 `Access-Control-Expose-Headers` |
| `always` 缺失 | 错误响应（4xx/5xx）不带 CORS 头 | 所有 `add_header` 加上 `always` |

18. 前端如何实现构建通知？构建成功/失败如何通知相关人员？钉钉、企业微信机器人如何集成？

## 构建通知集成

### 核心思路

CI/CD 流水线在构建成功/失败后，向 Webhook 发送 HTTP 请求，机器人推送消息到群聊。

---

## 一、钉钉机器人

```bash
# 钉钉群 → 群设置 → 机器人 → 添加自定义机器人 → 复制 Webhook URL
# 安全设置选"自定义关键词"，填入 "构建"
```

```bash
# scripts/notify-dingtalk.sh
WEBHOOK=$1
STATUS=$2   # success | failure
VERSION=$3
BRANCH=$4
ACTOR=$5

if [ "$STATUS" = "success" ]; then
  COLOR="✅"
  TITLE="构建成功"
else
  COLOR="❌"
  TITLE="构建失败"
fi

curl -X POST "$WEBHOOK" \
  -H "Content-Type: application/json" \
  -d "{
    \"msgtype\": \"markdown\",
    \"markdown\": {
      \"title\": \"$TITLE\",
      \"text\": \"## $COLOR $TITLE\n
**版本：** $VERSION\n
**分支：** $BRANCH\n
**触发人：** $ACTOR\n
**时间：** $(date '+%Y-%m-%d %H:%M:%S')\n
[查看详情]($RUN_URL)\"
    }
  }"
```

---

## 二、企业微信机器人

```bash
# 企微群 → 添加群机器人 → 复制 Webhook URL
curl -X POST "$WECOM_WEBHOOK" \
  -H "Content-Type: application/json" \
  -d "{
    \"msgtype\": \"markdown\",
    \"markdown\": {
      \"content\": \"## ✅ 构建成功\n
> 版本：<font color='info'>$VERSION</font>\n
> 分支：$BRANCH\n
> 触发人：$ACTOR\n
[查看构建日志]($RUN_URL)\"
    }
  }"
```

---

## 三、GitHub Actions 集成

```yaml
# .github/workflows/deploy.yml
name: Deploy

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: npm ci
      - run: npm run build
        id: build

      # 成功通知
      - name: 通知成功
        if: success()
        run: |
          bash scripts/notify-dingtalk.sh \
            ${{ secrets.DINGTALK_WEBHOOK }} \
            "success" \
            ${{ github.sha }} \
            ${{ github.ref_name }} \
            ${{ github.actor }}

      # 失败通知（加 @ 相关人员）
      - name: 通知失败
        if: failure()
        run: |
          curl -X POST "${{ secrets.DINGTALK_WEBHOOK }}" \
            -H "Content-Type: application/json" \
            -d '{
              "msgtype": "markdown",
              "markdown": {
                "title": "构建失败",
                "text": "## ❌ 构建失败\n**分支：** ${{ github.ref_name }}\n**触发人：** ${{ github.actor }}\n[立即查看](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})"
              },
              "at": {
                "atMobiles": ["138xxxxxxxx"],
                "isAtAll": false
              }
            }'
```

---

## 四、通知内容设计

```
✅ 成功通知（发群）：版本 + 分支 + 环境 + 构建链接
❌ 失败通知（发群 + @ 触发人）：失败原因 + 错误链接
⚠️ 回滚通知（发群 + @ 负责人）：回滚版本 + 原因 + 操作人
```

不同环境通知不同群：

```yaml
env:
  WEBHOOK: ${{
    github.ref_name == 'main' && secrets.PROD_WEBHOOK ||
    github.ref_name == 'develop' && secrets.DEV_WEBHOOK ||
    secrets.DEFAULT_WEBHOOK }}
```

---

## 总结

| 平台 | 协议 | 消息格式 | 安全验证 |
|---|---|---|---|
| 钉钉 | HTTP POST | Markdown / ActionCard | 关键词 / 签名 |
| 企业微信 | HTTP POST | Markdown / 图文 | Webhook Token |
| Slack | HTTP POST | Block Kit | Signing Secret |

核心只有一步：**CI 执行结果 → `curl` 打 Webhook**，其余都是消息格式的包装。

19. 什么是Serverless部署？Vercel、Netlify的部署原理是什么？

20. 前端如何实现构建产物的压缩？Brotli和Gzip的对比是什么？

21. 什么是边缘计算部署？Cloudflare Workers、Lambda@Edge如何使用？

22. 前端如何实现构建的依赖分析？circular-dependency-plugin如何检测循环依赖？

23. 什么是构建的预检查？pre-commit、pre-push钩子如何配置？
## 构建预检查 & Git Hooks

### 什么是预检查？

预检查是在代码**提交或推送之前**自动运行的校验流程，在问题进入代码库之前就拦截，而不是等到 CI/CD 才发现。

```
没有预检查：
  写代码 → commit → push → CI 跑 10 分钟 → 发现 lint 错误 → 返工

有预检查：
  写代码 → commit → 本地 1 秒检查 → 发现错误 → 立即修复
```

---

## 一、工具链

```
Husky        → 管理 Git Hooks 的工具，让 hooks 配置可以提交到仓库
lint-staged  → 只对暂存区（staged）文件运行检查，不扫全量，速度快
commitlint   → 校验 commit message 格式
```

---

## 二、安装配置

```bash
npm install -D husky lint-staged @commitlint/cli @commitlint/config-conventional
```

```bash
# 初始化 husky（在 .husky/ 目录生成钩子文件）
npx husky init
```

```json
// package.json
{
  "scripts": {
    "prepare": "husky"  // npm install 后自动初始化 husky，团队成员无需手动操作
  }
}
```

---

## 三、pre-commit 钩子

提交前对暂存文件做 lint + 格式化：

```bash
# .husky/pre-commit
#!/bin/sh
npx lint-staged
```

```js
// lint-staged.config.js
export default {
  // JS/TS 文件：先 eslint 修复，再 prettier 格式化
  '**/*.{js,jsx,ts,tsx}': [
    'eslint --fix --max-warnings=0',
    'prettier --write',
  ],

  // 样式文件：stylelint + prettier
  '**/*.{css,scss,less}': [
    'stylelint --fix',
    'prettier --write',
  ],

  // JSON / Markdown：只格式化
  '**/*.{json,md}': [
    'prettier --write',
  ],

  // 运行受影响的单元测试（可选，会慢一些）
  '**/*.{ts,tsx}': [
    () => 'tsc --noEmit',        // 类型检查（全量，不支持过滤文件）
  ],
}
```

---

## 四、commit-msg 钩子

校验 commit message 格式，强制团队统一提交规范：

```bash
# .husky/commit-msg
#!/bin/sh
npx commitlint --edit $1
```

```js
// commitlint.config.js
export default {
  extends: ['@commitlint/config-conventional'],

  rules: {
    // type 必须是以下之一
    'type-enum': [2, 'always', [
      'feat',     // 新功能
      'fix',      // bug 修复
      'hotfix',   // 紧急修复
      'docs',     // 文档
      'style',    // 格式（不影响逻辑）
      'refactor', // 重构
      'perf',     // 性能优化
      'test',     // 测试
      'build',    // 构建相关
      'ci',       // CI/CD
      'chore',    // 杂项
      'revert',   // 回滚
    ]],

    'type-case': [2, 'always', 'lower-case'],  // type 必须小写
    'subject-max-length': [2, 'always', 72],   // 描述不超过 72 字符
    'subject-empty': [2, 'never'],             // 描述不能为空
    'body-leading-blank': [1, 'always'],       // body 前需空行
  }
}
```

```bash
# ✅ 合法的 commit message
git commit -m "feat: 新增支付页面金额校验"
git commit -m "fix: 修复首页在 Safari 下样式错位 #123"
git commit -m "refactor(auth): 重构登录流程，拆分 token 管理模块"

# ❌ 会被拦截
git commit -m "修了个bug"
git commit -m "update"
git commit -m "Fix: 大写 Fix 不合规"
```

---

## 五、pre-push 钩子

推送前运行更重的检查（单测、构建验证）：

```bash
# .husky/pre-push
#!/bin/sh

BRANCH=$(git rev-parse --abbrev-ref HEAD)

echo "🔍 pre-push 检查（当前分支：$BRANCH）"

# main/develop 分支推送前必须通过单测
if [ "$BRANCH" = "main" ] || [ "$BRANCH" = "develop" ]; then
  echo "🧪 运行单元测试..."
  npm run test:unit -- --run  # vitest 单次运行（非 watch 模式）

  if [ $? -ne 0 ]; then
    echo "❌ 单元测试失败，push 已阻止"
    exit 1
  fi
fi

# TypeScript 类型检查
echo "📘 TypeScript 类型检查..."
npx tsc --noEmit

if [ $? -ne 0 ]; then
  echo "❌ 类型检查失败，push 已阻止"
  exit 1
fi

echo "✅ 所有检查通过，允许 push"
```

---

## 六、跳过钩子（紧急情况）

```bash
# 跳过 pre-commit（紧急 hotfix 时使用，事后必须补齐）
git commit --no-verify -m "hotfix: 紧急修复支付崩溃"

# 跳过 pre-push
git push --no-verify
```

> ⚠️ `--no-verify` 是紧急逃生通道，不能成为习惯。建议在团队规范中明确：使用 `--no-verify` 必须在 PR 描述中说明原因。

---

## 七、完整目录结构

```
项目根目录/
  ├── .husky/
  │     ├── pre-commit       ← lint-staged
  │     ├── commit-msg       ← commitlint
  │     └── pre-push         ← 单测 + 类型检查
  ├── lint-staged.config.js
  ├── commitlint.config.js
  ├── .eslintrc.js
  ├── .prettierrc
  └── package.json
```

---

## 总结

| 钩子 | 触发时机 | 适合做什么 | 耗时目标 |
|---|---|---|---|
| `pre-commit` | `git commit` 前 | lint、格式化（只检查暂存文件） | < 3s |
| `commit-msg` | 填写 message 后 | commitlint 格式校验 | < 1s |
| `pre-push` | `git push` 前 | 单测、TS 类型检查 | < 30s |
| CI Pipeline | push 到远端后 | 完整测试、构建、安全扫描 | 不限 |

核心原则：**本地钩子快速拦截低级错误，CI 做全量保障**，两者互补而不是互相替代。

24. 前端如何实现多地域部署？如何保证全球用户的访问速度？

## 前端多地域部署

### 核心问题

物理距离导致网络延迟，用户离服务器越远，访问越慢：

```
北京用户 → 北京节点：  ~5ms
北京用户 → 美国节点：  ~180ms
北京用户 → 欧洲节点：  ~250ms
```

多地域部署的目标是：**让每个用户都从距离最近的节点获取资源**。

---

## 一、整体架构

```
用户请求
    │
    ▼
DNS 智能解析（GeoDNS）
    │ 根据用户 IP 判断地域
    ├── 亚太用户  → 新加坡/香港节点
    ├── 欧洲用户  → 法兰克福节点
    ├── 北美用户  → 弗吉尼亚节点
    └── 南美用户  → 圣保罗节点
         │
         ▼
    CDN 边缘节点（静态资源）
         │ 未命中则回源
         ▼
    各地域源站（动态接口）
         │
         ▼
    全球数据库同步层
```

---

## 二、静态资源：全球 CDN 分发

前端静态资源（HTML/JS/CSS/图片）通过 CDN 天然实现多地域加速，这是最核心也最简单的一步。

### 主流 CDN 选型

| CDN 服务 | 适合场景 | 节点覆盖 |
|---|---|---|
| Cloudflare | 全球（尤其欧美） | 300+ 城市 |
| AWS CloudFront | 配合 AWS 生态 | 全球 |
| 阿里云 CDN | 中国大陆为主 | 全球 |
| 腾讯云 CDN | 中国大陆为主 | 全球 |
| Fastly | 欧美高性能场景 | 全球 |

### 国内外 CDN 分离策略

对于同时面向国内和海外用户的产品，单一 CDN 往往无法兼顾，常见做法是双 CDN：

```
                    DNS 智能解析
                         │
              ┌──────────┴──────────┐
         中国大陆用户           海外用户
              │                    │
        阿里云/腾讯云 CDN      Cloudflare CDN
        （ICP 备案，合规）      （全球节点优化）
              │                    │
              └──────────┬─────────┘
                     同一个源站
                  （或各地域源站）
```

### CDN 配置要点

```nginx
# 响应头配置 — 让 CDN 节点正确缓存
location ~* \.(js|css|woff2|png|webp)$ {
    # 带 hash 的资源，永久缓存
    add_header Cache-Control "public, max-age=31536000, immutable";
    
    # 允许 CDN 缓存（Cloudflare 等会读取此头）
    add_header Vary "Accept-Encoding";
    
    # 开启 gzip/br 压缩
    gzip on;
    gzip_types text/css application/javascript image/svg+xml;
    brotli on;  # Brotli 压缩比 gzip 更高
}

location = /index.html {
    # HTML 不缓存，保证用户总能拿到最新版
    add_header Cache-Control "no-cache, no-store, must-revalidate";
    
    # 不让 CDN 缓存 HTML
    add_header CDN-Cache-Control "no-store";
}
```

---

## 三、智能 DNS 调度

CDN 背后的流量调度依赖 DNS 层的地域识别，自建源站时也需要配置 GeoDNS：

```
# Route53 / Cloudflare 地理路由示例（伪配置）

api.example.com
  ├── 记录1: 39.x.x.x  → 生效地区: CN（中国）
  ├── 记录2: 52.x.x.x  → 生效地区: US（北美）
  ├── 记录3: 18.x.x.x  → 生效地区: EU（欧洲）
  └── 记录4: 13.x.x.x  → 默认（其他地区）
```

### DNS 层的健康检查与故障转移

```yaml
# AWS Route53 健康检查配置（示意）
HealthCheck:
  Type: HTTPS
  FullyQualifiedDomainName: ap-api.example.com
  Port: 443
  RequestInterval: 30      # 每 30s 检查一次
  FailureThreshold: 3      # 连续 3 次失败则标记不健康

# 关联路由策略
RoutingPolicy: Failover
  Primary:   ap-api.example.com   # 亚太主节点
  Secondary: us-api.example.com   # 故障时自动切换到北美
```

---

## 四、多地域源站部署

静态资源靠 CDN 解决了，动态 API 接口同样需要就近部署：

### 部署结构

```
Region: ap-southeast-1（新加坡）    ← 亚太主节点
  ├── Nginx / API Gateway
  ├── Node.js / Java 应用服务
  ├── Redis 缓存
  └── RDS（主库）
         │ 数据同步
         ▼
Region: eu-central-1（法兰克福）    ← 欧洲节点
  ├── Nginx / API Gateway
  ├── Node.js / Java 应用服务
  ├── Redis 缓存
  └── RDS（从库 / 只读）
         │
         ▼
Region: us-east-1（弗吉尼亚）      ← 北美节点
  ├── ...
  └── RDS（从库 / 只读）
```

### 前端接口请求就近路由

```ts
// src/utils/apiRouter.ts
// 前端根据用户时区或 IP 归属，选择最近的 API 节点

const REGION_ENDPOINTS = {
  'Asia/Shanghai':    'https://ap-api.example.com',
  'Asia/Tokyo':       'https://ap-api.example.com',
  'Asia/Singapore':   'https://ap-api.example.com',
  'America/New_York': 'https://us-api.example.com',
  'America/Chicago':  'https://us-api.example.com',
  'Europe/Berlin':    'https://eu-api.example.com',
  'Europe/London':    'https://eu-api.example.com',
} as const

function getNearestEndpoint(): string {
  const timezone = Intl.DateTimeFormat().resolvedOptions().timeZone

  for (const [tz, endpoint] of Object.entries(REGION_ENDPOINTS)) {
    if (timezone.startsWith(tz.split('/')[0])) {
      return endpoint
    }
  }

  // 兜底：默认节点
  return 'https://api.example.com'
}

export const API_BASE = getNearestEndpoint()
```

> 更可靠的方案是在 DNS/网关层做调度，前端时区判断只作为辅助兜底。

---

## 五、性能优化补充手段

多地域部署解决的是物理距离问题，以下手段进一步压榨性能：

### HTTP/2 + QUIC（HTTP/3）

```nginx
server {
    listen 443 ssl http2;   # HTTP/2 多路复用，减少连接开销
    listen 443 quic;        # HTTP/3 基于 UDP，弱网环境提升明显

    ssl_certificate     /etc/ssl/cert.pem;
    ssl_certificate_key /etc/ssl/key.pem;

    # 开启 0-RTT，复用会话加速握手
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 1d;
}
```

### 资源预加载 + 预连接

```html
<!-- 提前与 CDN/API 域名建立连接，消除 DNS + TCP 握手耗时 -->
<link rel="preconnect" href="https://cdn.example.com" crossorigin>
<link rel="preconnect" href="https://ap-api.example.com">
<link rel="dns-prefetch" href="https://cdn.example.com">

<!-- 预加载关键资源 -->
<link rel="preload" href="/assets/app.abc123.js" as="script">
<link rel="preload" href="/assets/app.3e7css.css" as="style">
```

### 图片自适应 — 按地区/网速分发不同规格

```ts
// 根据网络状况动态选择图片质量
function getImageUrl(path: string): string {
  const connection = (navigator as any).connection
  const effectiveType = connection?.effectiveType ?? '4g'

  const qualityMap: Record<string, string> = {
    'slow-2g': '?q=30&w=400',
    '2g':      '?q=50&w=600',
    '3g':      '?q=70&w=800',
    '4g':      '?q=85&w=1200',
  }

  const params = qualityMap[effectiveType] ?? '?q=85&w=1200'
  return `https://cdn.example.com${path}${params}`
}
```

### Service Worker 离线缓存

```ts
// sw.ts — 缓存关键资源到本地，弱网/断网也能访问
import { precacheAndRoute, cleanupOutdatedCaches } from 'workbox-precaching'
import { registerRoute } from 'workbox-routing'
import { NetworkFirst, CacheFirst, StaleWhileRevalidate } from 'workbox-strategies'

// 预缓存构建产物
precacheAndRoute(self.__WB_MANIFEST)
cleanupOutdatedCaches()

// HTML — 网络优先，离线降级到缓存
registerRoute(
  ({ request }) => request.mode === 'navigate',
  new NetworkFirst({ cacheName: 'html-cache' })
)

// JS/CSS — 缓存优先（有 hash，内容不变）
registerRoute(
  ({ request }) => ['script', 'style'].includes(request.destination),
  new CacheFirst({ cacheName: 'static-cache' })
)

// API — 先用缓存响应，后台更新（适合非实时数据）
registerRoute(
  ({ url }) => url.pathname.startsWith('/api/config'),
  new StaleWhileRevalidate({ cacheName: 'api-cache' })
)
```

---

## 六、监控：全球节点拨测

部署完成后需要持续监控各地域的实际访问质量：

```ts
// 前端性能上报 — 收集真实用户数据（RUM）
window.addEventListener('load', () => {
  const nav = performance.getEntriesByType('navigation')[0] as PerformanceNavigationTiming

  const metrics = {
    // DNS 解析耗时
    dns: nav.domainLookupEnd - nav.domainLookupStart,
    // TCP + TLS 握手
    tcp: nav.connectEnd - nav.connectStart,
    // 首字节时间（衡量服务器响应速度）
    ttfb: nav.responseStart - nav.requestStart,
    // 页面完全加载
    load: nav.loadEventEnd - nav.fetchStart,
    // 用户地域
    timezone: Intl.DateTimeFormat().resolvedOptions().timeZone,
    region: navigator.language,
  }

  // 上报到监控平台
  navigator.sendBeacon('/api/metrics', JSON.stringify(metrics))
})
```

---

## 总结

```
全球访问加速 = 静态资源 CDN 分发
             + DNS 智能地域调度
             + 多地域源站就近部署
             + HTTP/2 & QUIC 协议优化
             + Service Worker 本地缓存
             + 持续拨测监控
```

| 手段 | 解决的问题 | 收益 |
|---|---|---|
| 全球 CDN | 静态资源物理距离 | 最大，必做 |
| GeoDNS | API 接口就近路由 | 大，中大型产品必做 |
| HTTP/3 | 弱网/高延迟场景 | 中，优先级次之 |
| Service Worker | 重复访问、弱网 | 中，体验提升明显 |
| 图片自适应 | 流量与清晰度平衡 | 中，图片密集型产品必做 |
| 资源预连接 | 首屏连接建立耗时 | 小但零成本，建议都加 |

25. 什么是构建的产物拆分策略？vendor、common、runtime如何分离？

26. 前端如何实现发布审批流程？如何设计发布单和审批机制？

27. 什么是构建的性能监控？构建时间、产物大小如何持续追踪？

## 构建性能监控

### 为什么需要持续追踪？

```
不监控的后果：
  构建时间从 30s 悄悄涨到 5min → 没人知道是哪次提交引起的
  产物从 500KB 悄悄涨到 3MB  → 没人知道是哪个依赖引入的

持续监控的价值：
  每次 PR 都看到构建指标变化 → 问题在引入时就发现
```

---

## 一、构建时间监控

### Webpack 构建耗时分析

```bash
npm install -D speed-measure-webpack-plugin
```

```js
// webpack.config.js
const SpeedMeasurePlugin = require('speed-measure-webpack-plugin')
const smp = new SpeedMeasurePlugin({
  outputFormat: 'humanVerbose',   // 人类可读格式
  loaderTopFiles: 10,             // 显示耗时最长的 10 个 loader
})

module.exports = smp.wrap({
  // 原有 webpack 配置
  plugins: [...],
  module: { rules: [...] }
})

// 输出示例：
// ✔ babel-loader         18.3s  (342 files)
// ✔ css-loader            3.1s  (89 files)
// ✔ sass-loader           2.8s  (89 files)
```

### Vite 构建耗时

```ts
// vite.config.ts — 自定义插件记录各阶段耗时
function buildTimingPlugin() {
  const timings: Record<string, number> = {}

  return {
    name: 'build-timing',
    buildStart() {
      timings.start = Date.now()
      console.log('🚀 构建开始')
    },
    buildEnd() {
      timings.buildEnd = Date.now()
      console.log(`📦 打包完成：${timings.buildEnd - timings.start}ms`)
    },
    closeBundle() {
      const total = Date.now() - timings.start
      console.log(`✅ 构建总耗时：${total}ms`)

      // 写入文件，供 CI 读取
      require('fs').writeFileSync(
        'build-metrics.json',
        JSON.stringify({ buildTime: total, timestamp: Date.now() })
      )
    }
  }
}
```

---

## 二、产物大小监控

### bundlesize — PR 自动卡口

```bash
npm install -D bundlesize
```

```json
// package.json — 设置每个产物的大小上限
{
  "bundlesize": [
    { "path": "./dist/js/vendor.*.js",  "maxSize": "300 kB" },
    { "path": "./dist/js/app.*.js",     "maxSize": "100 kB" },
    { "path": "./dist/css/app.*.css",   "maxSize": "50 kB"  }
  ],
  "scripts": {
    "size": "bundlesize"
  }
}
```

```
超出限制时 CI 报错：
  ✗ dist/js/vendor.abc123.js
    282.3KB > maxSize 200KB (gzip)
    ↑ 超出 82.3KB，请检查新增依赖
```

### size-limit — 更精细的控制

```bash
npm install -D size-limit @size-limit/preset-app
```

```js
// .size-limit.js
module.exports = [
  {
    name: 'App JS',
    path: './dist/js/app.*.js',
    limit: '100 KB',
    gzip: true,
  },
  {
    name: 'Vendor',
    path: './dist/js/vendor.*.js',
    limit: '300 KB',
  },
  {
    name: 'CSS',
    path: './dist/css/*.css',
    limit: '50 KB',
  },
]
```

---

## 三、CI 持续追踪

```yaml
# .github/workflows/build-metrics.yml
name: Build Metrics

on: [push, pull_request]

jobs:
  metrics:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: 'npm'

      - run: npm ci

      # 记录构建时间
      - name: 构建并计时
        run: |
          START=$(date +%s%3N)
          npm run build
          END=$(date +%s%3N)
          echo "BUILD_TIME=$((END - START))" >> $GITHUB_ENV
          echo "构建耗时：$((END - START))ms"

      # 统计产物大小
      - name: 统计产物大小
        run: |
          TOTAL=$(du -sb dist/ | cut -f1)
          JS_SIZE=$(find dist -name '*.js' | xargs du -sb | awk '{sum+=$1} END{print sum}')
          CSS_SIZE=$(find dist -name '*.css' | xargs du -sb | awk '{sum+=$1} END{print sum}')

          echo "TOTAL_SIZE=$TOTAL"     >> $GITHUB_ENV
          echo "JS_SIZE=$JS_SIZE"      >> $GITHUB_ENV
          echo "CSS_SIZE=$CSS_SIZE"    >> $GITHUB_ENV

      # 大小限制检查（超出则 PR 不能合并）
      - name: Size Limit 检查
        run: npx size-limit

      # 在 PR 评论中展示指标对比
      - name: 评论构建指标
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `## 📊 构建指标报告
              | 指标 | 本次 |
              |------|------|
              | 构建耗时 | ${process.env.BUILD_TIME}ms |
              | 产物总大小 | ${(process.env.TOTAL_SIZE / 1024).toFixed(1)}KB |
              | JS 大小 | ${(process.env.JS_SIZE / 1024).toFixed(1)}KB |
              | CSS 大小 | ${(process.env.CSS_SIZE / 1024).toFixed(1)}KB |
              `
            })
```

---

## 四、历史趋势追踪

单次监控只能看当前，历史趋势才能发现渐进式劣化：

```ts
// scripts/save-metrics.ts
// 每次构建后将指标写入数据库或 JSON 文件

import fs from 'fs'
import path from 'path'

interface BuildMetrics {
  commit: string
  branch: string
  buildTime: number    // ms
  totalSize: number    // bytes
  jsSize: number
  cssSize: number
  timestamp: number
}

function saveMetrics(metrics: BuildMetrics) {
  const historyFile = 'build-history.json'
  const history: BuildMetrics[] = fs.existsSync(historyFile)
    ? JSON.parse(fs.readFileSync(historyFile, 'utf-8'))
    : []

  history.push(metrics)

  // 只保留最近 100 次
  const trimmed = history.slice(-100)
  fs.writeFileSync(historyFile, JSON.stringify(trimmed, null, 2))

  // 对比上次，超出阈值告警
  if (history.length > 1) {
    const prev = history[history.length - 2]
    const sizeIncrease = (metrics.totalSize - prev.totalSize) / prev.totalSize

    if (sizeIncrease > 0.1) {   // 产物增大超过 10%
      console.warn(`⚠️ 产物大小增加了 ${(sizeIncrease * 100).toFixed(1)}%！`)
      console.warn(`   上次：${prev.totalSize}B → 本次：${metrics.totalSize}B`)
    }
  }
}
```

---

## 总结

```
构建性能监控体系：

  构建时间 → speed-measure-webpack-plugin 定位慢 loader
           → CI 记录每次耗时，趋势异常告警

  产物大小 → bundlesize / size-limit 设置上限卡口
           → PR 自动评论指标对比
           → 历史数据追踪，发现渐进式膨胀
```

| 工具 | 作用 | 触发时机 |
|---|---|---|
| speed-measure-webpack-plugin | 定位构建慢在哪 | 本地排查 |
| bundlesize / size-limit | 产物大小卡口 | 每次 PR |
| CI 脚本 | 记录时间和大小 | 每次构建 |
| 历史 JSON / DB | 趋势分析 | 长期追踪 |

核心原则：**超出阈值 PR 不能合并**，把性能退化消灭在代码合并之前。

28. 前端如何实现热修复发布？紧急bug如何快速上线？

## 前端热修复发布

### 什么是热修复（Hotfix）？

热修复是指在**不走完整发布流程**的前提下，针对线上紧急 bug 快速修复并上线的机制。核心矛盾是：

```
常规发布：开发 → 测试 → staging → 审批 → 上线（可能需要数天）
热修复：  定位 → 修复 → 最小化验证 → 上线（目标 30 分钟内）
```

---

## 一、热修复的完整流程

### Git 分支策略

```
main/master（线上代码）
    │
    ├── hotfix/fix-payment-crash    ← 从 main 直接切出
    │       │
    │       ├── 修复代码
    │       ├── merge → main        ← 合并回主干，触发上线
    │       └── merge → develop     ← 同步回开发分支，避免下次发布覆盖修复
    │
    └── develop（日常开发）
```

```bash
# 标准 hotfix 操作流程

# 1. 从线上分支切出 hotfix 分支
git checkout main
git pull origin main
git checkout -b hotfix/fix-payment-crash

# 2. 修复 bug，提交
git add .
git commit -m "fix: 修复支付页面金额计算异常 #issue-123"

# 3. 合并回 main，打 tag
git checkout main
git merge hotfix/fix-payment-crash
git tag -a v1.2.1 -m "hotfix: fix payment crash"
git push origin main --tags

# 4. 同步回 develop，避免修复丢失
git checkout develop
git merge hotfix/fix-payment-crash
git push origin develop

# 5. 删除 hotfix 分支
git branch -d hotfix/fix-payment-crash
```

---

## 二、CI/CD 热修复流水线

热修复流水线和常规发布流水线的核心区别是**跳过耗时环节，保留关键卡点**：

```yaml
# .github/workflows/hotfix.yml
name: Hotfix Release

on:
  push:
    tags:
      - 'v*.*.*-hotfix*'   # 打特定格式 tag 触发
  workflow_dispatch:        # 支持手动触发
    inputs:
      skip_e2e:
        description: '跳过 E2E 测试（紧急情况）'
        type: boolean
        default: false

jobs:
  hotfix-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: 安装依赖（利用缓存）
        uses: actions/setup-node@v3
        with:
          node-version: 18
          cache: 'npm'

      - run: npm ci

      # 只跑关键单元测试，跳过完整 E2E
      - name: 单元测试（核心模块）
        run: npm run test:unit -- --testPathPattern="payment|auth|core"

      - name: 构建
        run: npm run build:prod
        env:
          VITE_API_BASE_URL: ${{ secrets.PROD_API_URL }}

      # 直接发布到生产，不经过 staging
      - name: 上传 CDN
        run: |
          ossutil cp -r ./dist oss://prod-bucket/frontend/ \
            --update --jobs 10

      - name: 刷新 CDN 缓存
        run: |
          aliyun cdn RefreshObjectCaches \
            --ObjectPath "https://cdn.example.com/index.html" \
            --ObjectType File

      - name: 通知钉钉/飞书
        run: |
          curl -X POST ${{ secrets.DINGTALK_WEBHOOK }} \
            -H "Content-Type: application/json" \
            -d '{"text": {"content": "🚨 热修复已上线：${{ github.ref_name }}"}}'
```

---

## 三、前端层面的紧急止血手段

在代码修复、构建、发布都需要时间的情况下，前端有几种**无需发版**的快速止血方案：

### 方案一：远程配置开关（功能降级）

提前在代码中埋好开关，紧急时直接后台关闭问题功能：

```ts
// src/config/remoteConfig.ts
// 应用启动时拉取远程配置，支持实时生效
class RemoteConfig {
  private config: Record<string, boolean> = {}

  async fetch() {
    const res = await fetch('/api/feature-flags')
    this.config = await res.json()
  }

  isEnabled(flag: string): boolean {
    return this.config[flag] ?? true // 默认开启
  }
}

export const remoteConfig = new RemoteConfig()

// 组件中使用
if (!remoteConfig.isEnabled('payment_new_flow')) {
  // 降级到旧流程
  return <OldPaymentFlow />
}
return <NewPaymentFlow />
```

```json
// 后台直接修改配置，0 秒生效，无需发版
{
  "payment_new_flow": false,  // ← 紧急关闭出问题的新功能
  "user_profile_v2": true
}
```

### 方案二：版本快速回滚

CDN 多版本共存，出问题时秒级切回上个版本：

```bash
# nginx 或 CDN 路由配置
# 只需修改 index.html 指向的资源版本路径

# 当前线上：v1.2.0
/frontend/v1.2.0/assets/app.abc123.js

# 出问题后，直接将 index.html 回滚到 v1.1.9 的内容
# 新资源（v1.2.0）仍然存在，不需要删除

# 脚本化回滚
./scripts/rollback.sh v1.1.9
```

```bash
#!/bin/bash
# scripts/rollback.sh
TARGET_VERSION=$1

echo "🔄 回滚到版本 $TARGET_VERSION"

# 从版本备份恢复 index.html
ossutil cp \
  oss://prod-bucket/backup/$TARGET_VERSION/index.html \
  oss://prod-bucket/frontend/index.html \
  --force

# 刷新 CDN 上的 index.html
aliyun cdn RefreshObjectCaches \
  --ObjectPath "https://cdn.example.com/index.html"

echo "✅ 回滚完成，当前版本：$TARGET_VERSION"
```

### 方案三：动态加载补丁脚本

适合修复量极小（几行代码）且无法立刻走构建流程的场景：

```html
<!-- index.html 预留补丁入口 -->
<script>
  // 从远端动态加载补丁，支持随时更新
  fetch('/api/patch-config')
    .then(r => r.json())
    .then(({ patchUrl, enabled }) => {
      if (enabled && patchUrl) {
        const script = document.createElement('script')
        script.src = patchUrl
        document.head.appendChild(script)
      }
    })
</script>
```

```js
// patch-v1.2.1.js — 上传到 CDN 的补丁文件
// 直接覆盖有问题的方法
import { paymentUtils } from '/assets/utils.abc123.js'

paymentUtils.calculateTotal = function(price, quantity) {
  // 修复：原来少乘了 quantity
  return price * quantity
}
```

> ⚠️ 补丁脚本是最后手段，维护成本高，上线后必须尽快用正式发版替代。

---

## 四、热修复的监控与验证

发布后的快速验证同样关键，不能盲目上线就完事：

```ts
// 错误监控：验证修复是否生效
// src/monitor/errorTracker.ts
window.addEventListener('unhandledrejection', (event) => {
  // 上报到 Sentry/自研监控
  reporter.send({
    type: 'unhandledRejection',
    message: event.reason?.message,
    stack: event.reason?.stack,
    version: __APP_VERSION__,  // 构建时注入的版本号
    timestamp: Date.now(),
  })
})

// 热修复上线后，在监控平台观察：
// 1. 错误率是否下降
// 2. 目标错误是否消失
// 3. 有无引入新错误
```

---

## 五、热修复流程总结

```
发现问题
    │
    ├── 能否立刻止血（不发版）？
    │       ├── 是 → 远程配置关闭功能 / 回滚版本  ← 优先
    │       └── 否 → 进入修复流程
    │
    ↓
从 main 切出 hotfix 分支
    │
    ↓
最小化修复（只改必要代码，不顺手重构）
    │
    ↓
核心模块单测通过
    │
    ↓
触发热修复流水线（跳过完整 E2E，直接构建上线）
    │
    ↓
上线后监控 5~15 分钟（观察错误率）
    │
    ├── 正常 → hotfix 分支合并回 develop，关闭 issue
    └── 异常 → 立刻回滚，重新分析
```

| 手段 | 生效速度 | 适用场景 | 风险 |
|---|---|---|---|
| 远程配置开关 | 秒级 | 新功能引发的 bug | 低 |
| 版本回滚 | 分钟级 | 整体功能异常 | 低 |
| 补丁脚本 | 分钟级 | 极小范围逻辑修复 | 中 |
| 热修复发版 | 15~30 分钟 | 需要代码修改的 bug | 中 |
| 常规发版 | 数小时起 | 非紧急问题 | 低 |

核心原则只有一条：**止血优先于完美，上线后持续观察，事后补充完整测试覆盖。**

## 场景题 (2题)

29. 你需要为一个日活千万级的电商平台设计前端构建和发布系统，需要支持多团队协作、频繁发布（每天数十次）、快速回滚、灰度发布、AB测试。你会如何设计？构建流程如何优化到最快？如何保证发布的稳定性和安全性？如何实现细粒度的灰度控制（按用户、地区、设备）？监控和告警如何设计？发布失败如何自动回滚？如何保证静态资源和HTML的版本一致性？CDN缓存如何处理？

## 日活千万级电商平台前端构建发布系统设计

---

## 一、整体架构

```
多团队代码
    │
    ▼
┌─────────────────────────────────────────┐
│           代码仓库层                      │
│  Monorepo（主应用 + 微前端子应用）          │
│  分支策略：feature → develop → release → main │
└─────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────┐
│           构建层                          │
│  分布式构建集群（并行构建多个子应用）         │
│  构建缓存（模块级 / 依赖级）               │
│  制品仓库（每次构建产物永久保存）            │
└─────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────┐
│           发布层                          │
│  灰度控制中心（用户/地区/设备维度）          │
│  AB 测试引擎                             │
│  蓝绿 + 金丝雀混合策略                    │
└─────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────┐
│           监控层                          │
│  实时指标采集 → 阈值判断 → 自动回滚         │
└─────────────────────────────────────────┘
```

---

## 二、构建优化：压缩到极限

### 模块联邦 — 多团队并行构建

```
不用模块联邦：
  所有团队代码 → 一次全量构建 → 15 分钟
  任何一个团队改代码都要等全量构建完

用模块联邦：
  商品团队 → 独立构建 ProductApp   → 2 分钟（并行）
  购物车   → 独立构建 CartApp      → 1.5 分钟（并行）
  订单团队 → 独立构建 OrderApp     → 2 分钟（并行）
  主应用   → 只构建 Shell          → 1 分钟
  总耗时：2 分钟（取最慢的那个）
```

```ts
// 主应用 shell — webpack.config.js
new ModuleFederationPlugin({
  name: 'shell',
  remotes: {
    // 运行时动态加载各团队的子应用
    product: 'product@https://cdn.example.com/product/remoteEntry.js',
    cart:    'cart@https://cdn.example.com/cart/remoteEntry.js',
    order:   'order@https://cdn.example.com/order/remoteEntry.js',
  },
  shared: {
    // React/Vue 等公共依赖只加载一次
    react: { singleton: true, requiredVersion: '^18.0.0' },
    'react-dom': { singleton: true },
  }
})

// 子应用 product — webpack.config.js
new ModuleFederationPlugin({
  name: 'product',
  filename: 'remoteEntry.js',   // 暴露入口
  exposes: {
    './ProductPage': './src/pages/ProductPage',
    './ProductCard': './src/components/ProductCard',
  },
  shared: { react: { singleton: true } }
})
```

### 构建缓存分层

```
Layer 1 — 依赖缓存（变化频率：周级）
  node_modules → 按 package-lock.json hash 缓存
  命中率：95%+，节省 npm install 时间 2~3 分钟

Layer 2 — 编译缓存（变化频率：天级）
  babel-loader cache / esbuild cache
  未修改的模块直接复用上次编译结果
  命中率：80%+，节省编译时间 60%

Layer 3 — 制品缓存（变化频率：按需）
  整个 dist/ 按 git tree hash 缓存
  代码完全没变（如只改了文档）→ 直接复用上次产物，构建耗时 0
```

```yaml
# GitHub Actions 缓存配置
- name: 缓存 node_modules
  uses: actions/cache@v4
  with:
    path: |
      node_modules
      ~/.cache/babel-loader
      .cache/webpack
    # 依赖 hash + 系统 + node 版本三重 key
    key: deps-${{ runner.os }}-node18-${{ hashFiles('package-lock.json') }}
    restore-keys: |
      deps-${{ runner.os }}-node18-

- name: 制品缓存（代码未变时跳过构建）
  id: artifact-cache
  uses: actions/cache@v4
  with:
    path: dist/
    key: artifact-${{ hashFiles('src/**', 'public/**', 'package-lock.json') }}

- name: 构建（缓存未命中才执行）
  if: steps.artifact-cache.outputs.cache-hit != 'true'
  run: npm run build
```

### 并行化流水线

```yaml
jobs:
  # 检查阶段全部并行，互不等待
  lint:
    runs-on: ubuntu-latest
    steps:
      - run: npm run lint

  typecheck:
    runs-on: ubuntu-latest
    steps:
      - run: npx tsc --noEmit

  test:
    runs-on: ubuntu-latest
    steps:
      - run: npm run test:unit -- --run

  # 多个子应用并行构建
  build-product:
    needs: [lint, typecheck, test]   # 检查通过才构建
    runs-on: ubuntu-latest
    steps:
      - run: npm run build --workspace=apps/product

  build-cart:
    needs: [lint, typecheck, test]
    runs-on: ubuntu-latest
    steps:
      - run: npm run build --workspace=apps/cart

  # 所有子应用构建完才发布
  deploy:
    needs: [build-product, build-cart, build-order]
    runs-on: ubuntu-latest
    steps:
      - run: bash scripts/deploy.sh
```

### 构建时间目标

```
优化前：全量构建 15 分钟
优化后：
  依赖缓存命中    → -3 分钟
  编译缓存命中    → -6 分钟
  并行构建        → -4 分钟
  目标：< 2 分钟（缓存热的情况下 < 30 秒）
```

---

## 三、发布稳定性与安全性

### 发布流程四道门

```
门1 — 自动化质量门禁（代码合并前）
  lint 通过 + 类型检查通过 + 单测覆盖率 > 80%
  产物大小未超限（size-limit）

门2 — 冒烟测试（部署 staging 后）
  核心链路自动化测试：首页加载 / 搜索 / 加购 / 下单 / 支付
  性能基准：LCP < 2.5s，FID < 100ms

门3 — 灰度观察期（小流量）
  5% 流量，观察 30 分钟
  错误率 / 转化率 / 性能指标与基线对比

门4 — 全量前人工确认（生产）
  值班同学在发布平台点击"确认全量"
  非工作时间禁止全量发布（系统拦截）
```

```ts
// 发布时间窗口控制
function isDeploymentAllowed(): { allowed: boolean; reason?: string } {
  const now = new Date()
  const hour = now.getHours()
  const day = now.getDay()

  // 禁止深夜发布
  if (hour < 9 || hour >= 20) {
    return { allowed: false, reason: '非工作时间（9:00-20:00）禁止发布' }
  }

  // 大促前 72 小时封板
  if (isPreSaleFreeze()) {
    return { allowed: false, reason: '大促备战期，代码已封板' }
  }

  return { allowed: true }
}
```

### 静态资源与 HTML 版本一致性

这是千万级平台最容易出问题的地方：HTML 更新了但用户拿的还是旧 JS。

```
错误顺序（会导致版本不一致）：
  1. 更新 index.html → 用户请求到新 HTML
  2. 上传新 JS/CSS   → 但此时旧 JS 还没替换完

正确顺序：
  1. 上传新 JS/CSS（带新 hash，与旧文件共存）← 旧用户不受影响
  2. 等待 CDN 同步确认（轮询各节点 hash）
  3. 更新 index.html（原子操作，此刻新版本对用户可见）
  4. 刷新 index.html 的 CDN 缓存
```

```ts
// scripts/deploy.ts — 严格控制发布顺序
async function deploy(version: string) {
  console.log('Step 1: 上传静态资源...')
  await uploadAssets(`dist/assets/`, `${version}/assets/`)

  console.log('Step 2: 验证 CDN 各节点同步...')
  await verifyCDNSync([
    'https://cdn1.example.com',
    'https://cdn2.example.com',
    'https://cdn3.example.com',
  ], `${version}/assets/app.${getHash()}.js`)

  console.log('Step 3: 原子更新 index.html...')
  await uploadFile('dist/index.html', 'index.html')

  console.log('Step 4: 刷新 HTML 缓存...')
  await refreshCDN(['https://cdn.example.com/index.html'])

  console.log('Step 5: 写入版本记录...')
  await recordVersion({
    version,
    timestamp: Date.now(),
    assets: getAssetManifest(),
  })
}

async function verifyCDNSync(nodes: string[], assetPath: string) {
  const maxRetries = 10
  for (let i = 0; i < maxRetries; i++) {
    const results = await Promise.all(
      nodes.map(node => fetch(`${node}/${assetPath}`, { method: 'HEAD' })
        .then(r => r.status === 200))
    )
    if (results.every(Boolean)) return
    await sleep(3000)
  }
  throw new Error('CDN 同步超时，发布终止')
}
```

---

## 四、细粒度灰度控制

### 灰度决策中心

所有灰度逻辑集中在一个服务，前端、Nginx、CDN 都向它询问"这个用户看哪个版本"。

```ts
// 灰度规则配置（存 Redis，实时生效）
interface GrayRule {
  ruleId: string
  version: string        // 灰度到哪个版本
  priority: number       // 规则优先级，数字越大优先级越高

  conditions: {
    // 用户维度
    userIds?: string[]           // 白名单用户
    userGroups?: string[]        // 内部员工 / VIP / 普通用户
    userIdHashRange?: [number, number]  // hash 取模，[0, 5] = 5% 用户

    // 地区维度
    regions?: string[]           // ['beijing', 'shanghai']
    countries?: string[]         // ['CN', 'US']

    // 设备维度
    devices?: ('mobile' | 'tablet' | 'desktop')[]
    os?: ('ios' | 'android' | 'windows' | 'mac')[]
    appVersionRange?: [string, string]  // App 内嵌 H5，按 App 版本

    // 时间维度
    timeRange?: [string, string]   // 只在某时间段灰度
  }

  traffic: number   // 命中条件后，再按此比例分流（0-100）
}

// 规则示例
const rules: GrayRule[] = [
  {
    ruleId: 'internal-first',
    version: 'v1.2.0',
    priority: 100,
    conditions: { userGroups: ['internal'] },  // 内部员工 100% 看新版
    traffic: 100,
  },
  {
    ruleId: 'beijing-5pct',
    version: 'v1.2.0',
    priority: 50,
    conditions: {
      regions: ['beijing'],
      devices: ['desktop'],
    },
    traffic: 5,   // 北京桌面用户中的 5%
  },
]
```

```ts
// 灰度决策函数
function resolveVersion(user: UserContext, rules: GrayRule[]): string {
  // 按优先级排序
  const sorted = rules.sort((a, b) => b.priority - a.priority)

  for (const rule of sorted) {
    if (matchConditions(user, rule.conditions)) {
      // 命中条件，再按 traffic 比例决定是否进灰度
      const hash = hashUserId(user.userId) % 100
      if (hash < rule.traffic) {
        return rule.version
      }
    }
  }

  return 'stable'  // 默认返回稳定版本
}

function matchConditions(user: UserContext, conditions: GrayRule['conditions']): boolean {
  if (conditions.userIds?.length && !conditions.userIds.includes(user.userId)) return false
  if (conditions.regions?.length && !conditions.regions.includes(user.region)) return false
  if (conditions.devices?.length && !conditions.devices.includes(user.device)) return false
  if (conditions.userGroups?.length && !conditions.userGroups.some(g => user.groups.includes(g))) return false
  return true
}
```

### Nginx 根据灰度决策路由

```nginx
# 灰度版本通过 Cookie 标记，Nginx 读取路由
map $cookie_app_version $html_path {
    "v1.2.0"  "/versions/v1.2.0/index.html";
    default   "/versions/stable/index.html";
}

server {
    location = / {
        # 向灰度服务查询当前用户应看哪个版本
        # 实际由接入层（如 Nginx Lua / Edge Worker）处理
        try_files $html_path /versions/stable/index.html;
    }
}
```

### AB 测试与灰度的协同

```ts
// AB 测试是灰度的超集，复用同一套规则引擎
interface ABExperiment extends GrayRule {
  variants: {
    name: string        // 'control' | 'treatment_a' | 'treatment_b'
    version: string     // 对应的前端版本
    traffic: number     // 各变体流量比例
  }[]
  metrics: string[]     // 观测指标：['cvr', 'add_to_cart', 'bounce_rate']
}

// 用户被分配到实验组后，记录到数据仓库
// 后续分析各指标在不同 variant 间的差异
function assignExperiment(user: UserContext, exp: ABExperiment): string {
  const hash = hashUserId(user.userId + exp.ruleId) % 100
  let cumulative = 0

  for (const variant of exp.variants) {
    cumulative += variant.traffic
    if (hash < cumulative) {
      // 打点记录实验分组
      track('experiment_assigned', {
        experimentId: exp.ruleId,
        variant: variant.name,
        userId: user.userId,
      })
      return variant.version
    }
  }

  return 'stable'
}
```

---

## 五、监控与自动回滚

### 监控指标分层

```
黄金指标（自动回滚触发）：
  JS 错误率     > 稳定版 2%          → 立即回滚
  接口 5xx 率   > 稳定版 1%          → 立即回滚
  核心转化率    下降 > 5%            → 立即回滚
  LCP           > 4s（P75）         → 告警 + 人工确认

业务指标（人工决策）：
  加购率、下单率、支付成功率
  页面停留时长、跳出率
  与灰度对照组实时对比

基础设施指标（参考）：
  CDN 命中率、带宽、错误码分布
```

```ts
// 前端监控 SDK — 区分版本上报
class Monitor {
  private version = __APP_VERSION__
  private isCanary = getCookie('app_version') !== 'stable'

  // 采集 Web Vitals
  collectWebVitals() {
    import('web-vitals').then(({ onLCP, onFID, onCLS }) => {
      onLCP(metric => this.report('lcp', metric.value))
      onFID(metric => this.report('fid', metric.value))
      onCLS(metric => this.report('cls', metric.value))
    })
  }

  // JS 错误采集
  collectErrors() {
    window.addEventListener('error', (e) => {
      this.report('js_error', {
        message: e.message,
        filename: e.filename,
        stack: e.error?.stack,
      })
    })

    window.addEventListener('unhandledrejection', (e) => {
      this.report('promise_error', { reason: String(e.reason) })
    })
  }

  private report(event: string, data: any) {
    navigator.sendBeacon('/api/monitor', JSON.stringify({
      event,
      data,
      version: this.version,     // 关键：带版本号，监控平台按版本聚合
      isCanary: this.isCanary,   // 区分灰度流量
      timestamp: Date.now(),
      userId: getUserId(),
      region: getRegion(),
    }))
  }
}
```

### 自动回滚引擎

```ts
// 监控服务端 — 持续计算指标，触发自动回滚
class AutoRollbackEngine {
  private checkInterval = 30_000  // 每 30 秒检查一次

  async startWatch(deployment: Deployment) {
    const baseline = await this.getBaselineMetrics('stable')

    const timer = setInterval(async () => {
      const canaryMetrics = await this.getMetrics(deployment.version)

      const decision = this.evaluate(canaryMetrics, baseline)

      if (decision.shouldRollback) {
        clearInterval(timer)
        await this.executeRollback(deployment, decision.reason)
      } else if (decision.shouldPromote) {
        clearInterval(timer)
        await this.promoteToNextStage(deployment)
      }
    }, this.checkInterval)
  }

  private evaluate(canary: Metrics, baseline: Metrics): Decision {
    const checks = [
      {
        name: 'JS错误率',
        value: canary.jsErrorRate - baseline.jsErrorRate,
        threshold: 0.02,
        rollback: true,
      },
      {
        name: '接口5xx率',
        value: canary.serverErrorRate - baseline.serverErrorRate,
        threshold: 0.01,
        rollback: true,
      },
      {
        name: '转化率下降',
        value: baseline.conversionRate - canary.conversionRate,
        threshold: 0.05,
        rollback: true,
      },
      {
        name: 'LCP劣化',
        value: canary.p75Lcp - baseline.p75Lcp,
        threshold: 500,  // 慢了 500ms
        rollback: false, // 只告警不自动回滚
      },
    ]

    const failedCheck = checks.find(c => c.rollback && c.value > c.threshold)
    if (failedCheck) {
      return {
        shouldRollback: true,
        reason: `${failedCheck.name} 超出阈值：${failedCheck.value.toFixed(4)} > ${failedCheck.threshold}`
      }
    }

    // 观察期结束且指标健康 → 提升流量
    if (this.isObservationComplete(canary) && this.allHealthy(checks)) {
      return { shouldPromote: true }
    }

    return { shouldRollback: false, shouldPromote: false }
  }

  private async executeRollback(deployment: Deployment, reason: string) {
    console.error(`🚨 自动回滚触发：${reason}`)

    // 1. 切换灰度规则，将该版本流量归零
    await grayService.disableVersion(deployment.version)

    // 2. 还原 index.html 到上个稳定版本
    await ossClient.copy(
      `versions/${deployment.prevVersion}/index.html`,
      'index.html'
    )

    // 3. 刷新 CDN
    await cdnClient.refresh(['https://cdn.example.com/index.html'])

    // 4. 告警通知（@所有相关人）
    await notify({
      level: 'critical',
      message: `🚨 生产自动回滚：${deployment.version} → ${deployment.prevVersion}\n原因：${reason}`,
      atAll: true,
    })

    // 5. 写入事故日志
    await incidentLogger.record({ deployment, reason, timestamp: Date.now() })
  }
}
```

---

## 六、CDN 缓存处理

```
资源分类策略：

带 hash 的 JS/CSS/图片：
  Cache-Control: max-age=31536000, immutable
  永不过期，内容变则 hash 变，天然无缓存问题

index.html：
  Cache-Control: no-cache
  每次都向服务器验证（304 Not Modified 机制复用缓存）
  绝不设置强缓存

remoteEntry.js（模块联邦入口）：
  Cache-Control: no-cache
  与 index.html 同等对待，必须实时获取

version.json（版本信息文件）：
  Cache-Control: no-store
  完全不缓存，用于前端检测版本更新
```

```bash
# 发布脚本中的 CDN 刷新策略
# 只刷新 no-cache 资源，带 hash 的资源不需要刷新

REFRESH_PATHS=(
  "https://cdn.example.com/index.html"
  "https://cdn.example.com/product/remoteEntry.js"
  "https://cdn.example.com/cart/remoteEntry.js"
  "https://cdn.example.com/version.json"
)

for path in "${REFRESH_PATHS[@]}"; do
  aliyun cdn RefreshObjectCaches --ObjectPath "$path" --ObjectType File
done
```

---

## 七、多团队协作规范

```
权限设计：
  开发同学    → 可触发 test / staging 发布
  Tech Lead  → 可触发生产灰度发布（5%）
  值班负责人  → 可触发全量发布 / 紧急回滚

发布隔离：
  各团队子应用独立发布，互不阻塞
  主应用发布需要各子应用负责人确认

变更记录：
  每次发布自动记录：操作人 / 版本 / diff / 影响范围
  便于事后复盘和审计
```

---

## 总结

```
构建速度：模块联邦并行 + 三层缓存 → 目标 < 2 分钟
发布稳定：四道质量门 + 严格发布顺序 → 版本一致性保障
灰度控制：统一决策中心 → 用户/地区/设备多维度精准控制
自动回滚：30 秒一次指标评估 → 分钟级自动止损
CDN 策略：带 hash 永久缓存 + HTML no-cache + 发布只刷 HTML
```

这套体系的核心思想只有一条：**把每一个可能出错的环节都系统化**——人工决策的地方尽量自动化，自动化的地方保留人工覆盖，任何变更都有记录和回滚路径。

30. 假设你要为一个国际化SaaS产品设计全球化部署方案，需要在全球多个地区（北美、欧洲、亚洲）部署，需要满足数据合规（GDPR等）、低延迟访问、高可用性。你会如何设计部署架构？如何实现多地区的构建和部署？CDN和边缘节点如何规划？如何处理不同地区的配置差异？如何保证各地区版本的一致性？跨地区的监控和日志收集如何实现？灾备和故障转移方案是什么？如何优化全球用户的首屏加载时间？

为国际化SaaS产品设计全球化前端部署方案，需兼顾**低延迟、高可用、数据合规**。以下从部署架构、构建分发、配置管理、监控、灾备及性能优化六个维度阐述。

---

## 一、部署架构：全球多区域 + CDN + 边缘计算

### 1.1 静态资源全球分发
- **核心**：使用全球CDN（如Cloudflare、Akamai、AWS CloudFront）分发静态资源（HTML、JS、CSS、图片）。CDN边缘节点就近缓存，大幅降低延迟。
- **源站策略**：在**北美、欧洲、亚洲**分别部署源站（如AWS S3桶或自建Nginx集群），CDN回源时通过**地理DNS**或CDN的**多源站**功能，自动选择最近的源站。欧洲源站必须位于欧盟境内，以满足GDPR数据本地化要求。

### 1.2 动态内容与BFF
- **API/BFF层**：在每个区域独立部署BFF服务（如Node.js集群），负责聚合后端数据、处理区域化业务逻辑。BFF与后端核心服务通过内部专线通信，或使用多活数据库。
- **数据合规**：欧洲区域BFF仅调用欧洲内部服务，确保用户数据不离境；亚洲同理。后端数据库按区域拆分（如欧洲数据库、亚洲数据库），全球数据同步由后端负责，前端无感知。

### 1.3 边缘计算
- 利用CDN的**边缘计算能力**（如Cloudflare Workers、Lambda@Edge）执行轻量级逻辑：
  - 根据用户IP判断所属区域，动态返回对应区域的配置或实验分组。
  - 在边缘进行A/B测试、重定向、鉴权，减少到源站的请求。

---

## 二、多地区构建与部署

### 2.1 构建流程
- **CI/CD**：使用GitHub Actions/GitLab CI触发构建。每次合并到主分支生成唯一版本号（如 `build-{timestamp}-{commit}`）。
- **产物**：构建后得到静态资源（带内容哈希）和版本清单（`asset-manifest.json`）。所有资源上传至**中心存储**（如AWS S3全局桶），供各区域源站同步。

### 2.2 同步到各区域源站
- 通过**跨区域复制**（如S3跨区域复制）将中心桶的产物自动同步到北美、欧洲、亚洲的源站桶。或使用CDN的多源站直接回源中心桶，但为降低延迟和合规，建议区域源站。
- **版本一致性**：所有区域源站保存同一版本的文件（哈希文件名保证唯一性），HTML入口文件通过部署时更新引用路径，确保各区域加载的静态资源完全一致。

### 2.3 部署与发布
- **蓝绿部署**：每个区域源站保留当前版本和候选版本目录，通过切换符号链接或更新CDN回源路径实现零停机发布。
- **灰度发布**：结合边缘计算，按用户ID、地域等标签，将部分流量导向新版资源（如返回不同HTML入口）。逐步放量，观察监控指标。

---

## 三、地区配置差异管理

### 3.1 动态配置中心
- 使用**配置中心**（如Apollo、Nacos）或**边缘KV存储**（如Cloudflare Workers KV）存储每个区域的配置（语言包、货币、主题、功能开关）。
- 前端启动时，通过API获取区域配置（可携带用户IP或语言头），或在构建时将配置嵌入HTML但需多版本构建，推荐动态获取，避免重复构建。

### 3.2 个性化配置
- 通过边缘计算注入配置：边缘节点根据用户地理位置，将配置直接写入HTML内联脚本（`window.__REGION_CONFIG__`），首屏即可使用，无需额外请求。

---

## 四、跨地区监控与日志

### 4.1 真实用户监控（RUM）
- 部署RUM探针（如自研sdk或Datadog RUM），采集各区域的**Web Vitals**（FCP、LCP、CLS）、API延迟、错误率。
- 数据按区域打标，上报到中心化分析平台（如自建ClickHouse或云服务）。需在欧洲区域保留一份日志副本以满足合规。

### 4.2 错误追踪
- 使用Sentry或自建错误监控，配置多区域DNS，让用户上报到最近的中转服务器，再聚合到中心。

### 4.3 告警
- 设置多级告警：当某区域错误率突增、延迟超过阈值时，通过钉钉/邮件通知，并触发自动回滚。

---

## 五、灾备与故障转移

### 5.1 多区域故障转移
- **DNS层面**：使用全局负载均衡（如AWS Route 53），配置健康检查。当某区域源站或BFF故障时，自动将流量切换到另一健康区域。
- **CDN层面**：CDN本身具备冗余，若一个源站不可用，可自动回源到其他源站（需配置多源站）。

### 5.2 静态资源灾备
- 各区域源站之间互为备份。若欧洲源站故障，CDN可回源到北美源站（需考虑合规，可临时允许，但需评估）。

### 5.3 自动回滚
- 发布后监控指标，若错误率或核心业务指标下降超阈值，CI/CD自动触发回滚：切换CDN回源路径或更新DNS指向旧版本。

---

## 六、全球用户首屏加载优化

1. **CDN边缘缓存**：静态资源设置长时间缓存（`Cache-Control: public, max-age=31536000, immutable`），用户从最近边缘节点获取。
2. **边缘渲染**：对首屏HTML使用边缘计算动态生成（如Next.js的Edge SSR），结合区域配置，返回直接可渲染的HTML，消除白屏。
3. **资源预加载**：使用`<link rel="preconnect>`、`<link rel="dns-prefetch>`加速跨域连接，`<link rel="preload>`加载关键资源。
4. **图片优化**：使用WebP/AVIF格式、响应式图片、懒加载。
5. **代码分割**：按路由拆分JS，首屏只加载必要代码。
6. **区域化CDN加速**：确保每个区域的CDN节点覆盖全面，对于亚太地区，考虑使用当地CDN服务商（如阿里云CDN）作为补充。

---

## 七、总结

该方案通过全球CDN分发、区域化源站、边缘计算、动态配置、全链路监控及自动化故障转移，实现了**低延迟、高可用、合规**的全球化前端部署。版本一致性通过统一构建和跨区域同步保证，配置差异在边缘动态注入，首屏性能达到最优。此架构可支撑千万级日活的国际化SaaS产品平稳运行。
